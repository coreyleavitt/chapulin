# Verification Harness v2 — handoff

- **Stage:** 4 (code-review) — **Round 2 COMPLETE, FLOOR REACHED (2026-08-24): 0 Critical/High/Medium remain.** Fresh 4-reviewer pass → 2 High + 5 Medium (all prod `src/`) + Lows; fix loop closed H1/H2/M1/M2/M3/D1/L1 (all VERIFIED green: full default suite + both out-of-suite `nim check`s + symex-decode in isolation), M4 DEFERRED with sound reason; a clean 4-dimension re-review surfaced no new Critical/High/Medium (only 3 new Lows). See Review ledger → "Round 2 review ledger". **ALL LOWS NOW RESOLVED (2026-08-24, Corey said "fix all the lows"): L1–L11 each fixed / no-action / wontfix — see the Low-outcomes block appended to the Round 2 ledger. Only M4 remains deferred (sound reason).** **NOTHING COMMITTED — awaiting Corey.** **Part A 7/7 ✅, Part B 4/4 ✅, Part C 6/6 ✅, Docs ✅** (impl stages complete).   •   **Round:** 2 (done).
- **TOOLCHAIN NOW: `nelli` v0.5.3** (proptest was RENAMED to nelli; every test `import proptest` is now `import nelli`). Bumped 0.3.2→v0.5.3 on **2026-08-24**: milpa.kdl dev-dep + milpa.lock re-locked (`milpa -C <proj> lock` then `fetch` with `MILPA_CACHE_DIR=<proj>/.milpa-cache`), `_deps/nelli` fetched in-tree, nim.cfg `--path:"_deps/nelli/src"`. Only nelli moved; z3/NiGui/softlink pins unchanged. **Full default suite (35) + ALL symex opt-in suites re-verified GREEN under nelli 0.5.3, 2026-08-24** (`t_server`/`t_symex_smoke` each hit ONE transient Windows-container mount `OSError: corrupted and unreadable` during codegen → GREEN on retry; known shared-mount flakiness, retry don't debug). Intervening B7 round unified the decode symex twin (all 6 arms, `walker v88`); A6 round tightened `writeSidecar` back to `sxUnsat`.
- **2026-08 proptest upgrade (0.0.1→0.1.0, nim-z3→main, softlink→v0.11.1):** every symex workaround in `tests/t_symex*.nim` was re-evaluated against the natural/shipped-proc form; full verdicts (per catalog #1-#11 + fuzz/corpus items) are in `docs/proptest-findings.md`'s "2026-08 re-evaluation" section. **B4 UNBLOCKED AND DONE** — `parseBiggestInt` is now modeled (routes to the same IR as `parseInt`); `t_symex.nim`'s tsize twins call it directly, no substitution. Also removed: Blocker #2 (tuple-constructor return, `t_symex_decode.nim`), #7 (`toLowerAscii`, `t_symex_uri.nim`), #8 (`rfind`, `t_symex_uri.nim`), the `system.min`/`max` inlining workaround (`t_symex.nim`), and the in-place `.add` workaround (`t_symex_security.nim`'s `isWithinTwin`). Blocker #9 was refined (not fully fixed — a loop-produced value survives fine in a same-statement chain, not once bound to a name across statements); #3 and #6 are STILL PRESENT and got WORSE in one shape each (both now crash natively instead of degrading to `sxUnknown`) — see the findings doc for the exact repros. All seven `t_symex*` suites + the full default suite re-verified GREEN after every change.
- **Resume (Stage-4 Round 2 — a FRESH review pass):** re-run `/code-review docs/rfc/verification-harness-v2.md` against the CURRENT tree — launch the 4 standing reviewers (correctness, security, design & ergonomics, verification-validity) with `model: sonnet, run_in_background: true`, adversarially verify, present, then run the fix loop through Medium under the already-approved mandate. **Fold in the two KNOWN-OPEN items** (verify they still hold, then fix): **S1** — `server.nim:913-914,932-933` `onRejected` called UNWRAPPED inside `run()`'s accept loop (a throwing callback stops the listener; site 1 at :758-759 is isolated inside the `handleRequest` future, sites 2/3 are not); **D1** — the reject block (mint→sendError→close→onRejected) is DUPLICATED 3× at `server.nim:~752/:908/:927`. Both close with one `proc rejectRequest(server, clientHost, clientPort, code, msg) {.async.}` wrapping `onRejected` in `try/except CatchableError`. **Already FIXED+VERIFIED this session (do NOT redo):** C1 (`src/chapulin.nim` serve-loop `evServerRejected` arm — `nim check` clean) and C2 (`gui/desktop/chapulin_gui.nim` `evServerRejected` arm — `nim check -d:withGui` clean). **DISSOLVED:** V1 (writeSidecar now sxUnsat; decode B7). **Treat A4H/VM1/VM2/D2/S2/S3/L1–L3 as STALE — re-verify before acting** (A6/B7 touched the symex security/uri suites + harness).
- **Round 2 review ledger (2026-08-24 — fresh 4-reviewer pass + fix loop + clean re-review; FLOOR REACHED: 0 Critical/High/Medium remain):** **H1** `t_symex_decode.nim` `readOptionsTwin` drift → concrete `diffOracleCheck` over-claimed full equivalence (falsifiers `@[0,6,0,'A',0]`/`@[0,6,'k',0]`) → **FIXED+VERIFIED** (Batch C): added a separate `readOptionsOracle` byte-for-byte faithful to real `readOptions`, used only by `decodeFullTwin`; symex membership twin left intact & honestly scoped; 2 falsifier vectors added (RED-confirmed), full symex suite green in isolation. Re-review (verif-validity) traced ALL decodeFullTwin arms faithful, not just options. **H2** (=S1) unwrapped `onRejected` in `run()` loop → 1 pre-`decode` datagram kills listener/process → **FIXED+VERIFIED** (Batch A): new `proc rejectRequest(server,host,port,code,msg){.async.}` wraps `onRejected` in `try/except CatchableError`; RED test (throwing callback → `activeTransfers==0`, loop dead) → GREEN (`==1`, loop survives) in `t_a4_maxconcurrent.nim`. **M1** `onRejected` 4 scalars → **FIXED** (`RejectInfo` struct, matches `TransferInfo` convention). **M2** `snap` common-field plausible-sentinel → **FIXED+VERIFIED** (Batch D): `snap`+`errorCode`+`errorMsg` moved into a MERGED `of evTransferStarted,Progress,Complete,Error:` arm (Nim forbids dup field name across branches → single merged arm is the only shape keeping one `ev.snap` accessor); now a COMPILE ERROR on all 5 server kinds. Surfaced+fixed a real sentinel reliance (`t_eventqueue.mkLog` set bogus `snap:`). Both `nim check`s (CLI+GUI, outside dev-test compile set) clean; full default suite green. **M3** `evServerRejected` ∉ `ProtectedKinds` → dropped under A4 saturation → **FIXED+VERIFIED** (Batch B): added to `ProtectedKinds`; 2 non-vacuous `t_eventqueue` tests (retention-under-cap w/ specific rej* field asserts; protected-flood never exceeds cap). Cap invariant re-confirmed by existing D6 opseq property. **M4** `transferFactory(0)` 1-shot-ERROR shape → **DEFERRED (sound)**: a `sendOneOff` primitive would need a 2nd mockable factory seam that breaks `WireRegistry`'s call-order-index contract shared by 7 test files — exceeds a clean 1-attempt fix; D1 consolidation localized `transferFactory(0)` to one call site regardless. Re-review (design) confirmed no cleaner localized option exists. **D1** reject block dup 3× → **FIXED** (the `rejectRequest()` vehicle). **L1** `transferFactory` nil-deref at reject sites → **FIXED** (nil-guard folded into `rejectRequest`). **Re-review = 4 dimensions CLEAN, no new Critical/High/Medium.** **Remaining Lows (reported, not fixed — below mandate):** L2 `interopcapture.nim:120` `doAssert magic` raises `AssertionDefect` on pcapng/non-classic-pcap (contradicts "skip don't raise"; opt-in CLI only); L3 `interopcapture.nim:129` mid-stream bad `incl_len` drops later intact records; L4 `corpusmin.nim` doc rationale stale vs nelli 0.5.3 (`minimalCovering` now exported; runtime still sound via `unionCoverageOf`); L5 `evServerLog.sMessage` unsanitized at chapulin.nim:267/gui:291 (latent, no live escape via reject paths); L6 reject reflection/amp (PRE-EXISTING, not this diff); L7 `fuzzProperty` macro-shape doc pointer; L8 `UdpListener` no `send` (info); L9 (NEW, re-review) `allocateTransferTransport` calls `transferFactory` WITHOUT the nil-guard L1 added to reject sites — embedder-misconfig only, not attacker-reachable, consistency follow-up; L10 (NEW) `RejectInfo.clientHost` vs `Event.rejClientHost` naming wart (rej- prefix not forced by collision); L11 (NEW, cosmetic) `t_eventqueue` M3 test docstring says "a log event is evicted" but 2 are. **LOW OUTCOMES (2026-08-24, "fix all the lows" — all closed, 2 serial sonnet subagents, code batch then docs batch, both GREEN):** **L1** → FIXED in the Round-2 fix loop (nil-guard folded into `rejectRequest`). **L2** → FIXED: `interopcapture.nim` `extractUdpPayloads` replaced `doAssert magic == 0xa1b2c3d4` with graceful `if magic != …: return (payloads, 0)` (pcapng/big-endian degrade to empty, no `AssertionDefect`); dropped now-unused `std/strutils`; regression test with pcapng magic added; verified via `t_interop_capture`. **L3** → FIXED: same proc now returns `tuple[payloads, unconsumedBytes]`, emits a `stderr` diagnostic and reports unconsumed count when the record loop stops with `pos < len` (trailing OR mid-stream `incl_len` corruption); doc updated (mid-stream corruption unrecoverable by construction, no resync); `harvestPcapBytes` destructures; regression test corrupts a middle record and asserts the intact first record survives + the loss signal fires; verified via `t_interop_capture` (diagnostic observed firing). **L4** → FIXED (doc-only, stale vs nelli 0.5.3, verified against `.milpa-cache/dag-sha256/ed53d…3f8` = `_deps/nelli` symlink target, `nelli.nimble` version 0.5.3): `minimalCovering*` IS now exported (`fuzz.nim:368`) and `fuzz()` now runs an up-front coverage-replay pass over preloaded seeds (`fuzz.nim` ~459-520, tag F2), so `minimizeCorpus` losslessly reduces external corpora; rewrote `corpusmin.nim` module + `minimizeGrownCorpus` doc comments as resolved-in-0.5.3; verified via `t_corpus_minimize`. **L5** → FIXED: wrapped `ev.sMessage` in `sanitizeForDisplay(...)` at both `evServerLog` display sites (`src/chapulin.nim:267`, `gui/desktop/chapulin_gui.nim:291`), matching the sibling `errorMsg`/`startErr` convention; `nim check` clean on CLI + `-d:withGui` GUI. **L6** → FIXED (doc-only): added a bullet under SECURITY.md "Residual limitations (known and accepted)" — spoofed-source ERROR-reply reflection is a bounded (~1:1, non-amplifying, unlike DNS/NTP/memcached) vector, mitigation is rate-limiting/network placement (grep confirmed no prior amplif/reflect coverage). **L7** → FIXED (doc-only): `fuzzsupport.nim`'s `fuzzProperty` doc comment now cites the concrete macro site `nelli/dsl.nim:23` (`macro property*`), body indexing `dsl.nim` ~37-56, error site `dsl.nim:42`. **L8** → NO ACTION (informational: `UdpListener` has no `send` — by design). **L9** → FIXED: nil-guard on `server.transferFactory` at the top of `allocateTransferTransport` (`server.nim:720`), returns `(Transport(), false)` — the same not-bound shape the port-range-exhausted path uses; comment notes it mirrors `rejectRequest`'s guard; verified via `t_a4_maxconcurrent`/`t_server`/`t_session`. **L10** → WONTFIX (rationale: `RejectInfo.clientHost` vs `Event.rejClientHost` — the `Event` union already uses per-arm prefixes; dropping `rej` would make `evServerRejected` the inconsistent outlier). **L11** → FIXED (docstring-only): `t_eventqueue` M3 test renamed from "a log event is evicted" to note two logs are evicted (one for the event itself, one for the pending drop-count warning); assertions untouched; verified via `t_eventqueue`. **NET: 0 Critical/High/Medium/Low open; only M4 deferred. Still NOTHING COMMITTED.** **PASSES (both rounds):** all symex twins (sec/uri/checksum/netascii) honest+divergences asserted; soak/coverage anti-vacuity real; t_a4 asserts literal activeAtReject==maxConcurrent; pcap bounds-checking sound. **NOTHING COMMITTED.** Whole v2 tree still uncommitted; stage list in the CONTEXT line below now also includes the round-2 fix touches (server/api/eventqueue/chapulin.nim/gui + t_a4/t_eventqueue/t_symex_decode).
- **CONTEXT for Round 2:** `src/chapulin.nim` and `gui/desktop/chapulin_gui.nim` are OUTSIDE `dev-test.ps1`'s compile set (it builds only `tests/`), so enum/API changes to `eventqueue.EventKind` can break them invisibly — Round 2 should `nim check` both as part of any `EventKind`-touching finding. Run suites via `pwsh scripts/dev-test.ps1 [-Only "a,b,c"]` (comma-STRING form for multiple; the `@('a','b')` array literal misbinds to `-Soak`/`-Image` when invoked as a fresh `pwsh` process). **Whole v2 tree is UNCOMMITTED** (stacks on committed BlockSource work); commit only when Corey asks. When committing, MUST stage: the symex `.nim.cfg` sidecars, `Dockerfile.pcap`, `scripts/lib/nimcontainer.ps1`, all new `tests/*.nim`, `tests/corpus/*.soak-corpus.bin`, the A4 `src/` trio (server/eventqueue/api.nim), the C1/C2 fixes (`src/chapulin.nim` + `gui/desktop/chapulin_gui.nim`), and the nelli bump (`milpa.kdl`/`milpa.lock`). **NEVER run a background full-suite verification while a subagent runs tests — concurrent `nim c` into the shared mount collides; verify serially. On a transient `OSError: corrupted and unreadable`, just retry the one suite.**
- Docs ✅ (2026-07-12) — SECURITY.md Verification table + README, no `src/`. No machine-checked table test exists (verified by grep) → cited testIds checked by grep/read + confirmed byte-for-byte against live `[Suite]` output. **SECURITY.md:** FLIPPED api.nim-facade row → `t_a2_facade_stateful.nim` (A2 StateMachine + A3 `facade.hostileInjection` + narrow waitTransfer/waitServer), backed by `t_defect_canary.nim`+`t_asynccheck_tripwire.nim` (Wire-mocked scope noted); FLIPPED maxConcurrent row → `t_a4_maxconcurrent.nim` (keyed on `server.activeTransfers`, documents the onRejected/evServerRejected prod change); ADDED B rows with HONEST bounds (`t_symex_decode` header+first-scan-only; `t_symex_security`+`t_symex_uri` pre-I/O twins w/ divergences; `t_symex_checksum`; `t_symex_netascii` exhaustive-512-state); ADDED 3 C rows (soak/min/provenance; coverage-progress `t_coverage_report`/`-CorpusReport` w/ the honest 13→13 converged-corpus demo; interop-capture w/ Windows-scope-down + `-unverified` atftp tag). **README:** new Facade-stateful-property para + expanded symex para (bounded-decode caveat) + Soak-campaign para (local-opt-in, converged-may-show-zero-growth, interop env limit). **B4 correctly left unclaimed anywhere** (gated). Default suite green (34) + symex suites green.
- C5 ✅ (2026-07-12) — coverage-progress signal + `-CorpusReport` verb, test-tooling only, no `src/` changes. New `tests/coveragereport.nim`: `coverageDelta*[T](s, prop, beforeEntries, afterEntries): CoverageDelta` (`{before, after: int}` + `expanded*(d): bool = d.after > d.before`) and `committedCorpusCoverage*[T](s, prop, db, testId): int` — BOTH built entirely on C4's already-exported `corpusmin.nim` primitives (`unionCoverageOf`/`coveredEdgeCount`), reused verbatim per the task's explicit "REUSES C4's coverage-measurement path" instruction — no new coverage-measurement mechanism invented. **Re-scoping re-verified against actual proptest source** (not re-trusted from the RFC's prose): `coverage.nim`'s `{.cover.}` macro (`edgeIdFromLineInfo`) hashes `file:line:col` INTO a bitmap-slot id at compile time and never records the inverse — confirmed there is genuinely no slot→source map to build the deferred §8 report from without new compile-time tooling; `snapshotCoverage`/`Coverage.counters`/`coverageEdgeCount` (all real, exported, reached via plain `import proptest` since `fuzz.nim` does `export coverage`) are exactly and only the coarse per-slot counts the RFC scoped C5 to. **RED-GREEN acceptance test** `tests/t_coverage_report.nim` (registered in `scripts/dev-test.ps1`'s default `$tests`): a local, self-contained synthetic `if/elif/elif/elif/else` target (`syntheticUnderCovered`, own copy, deliberately independent of `t_corpus_minimize.nim`'s `syntheticBranchy` fixture so this suite's under-coverage story never silently drifts if that file changes) — `before` = 2 fixed IR seeds hitting only the first two arms, `after` = `before` plus one seed hitting a third, previously-unreached arm; asserts `delta.before == 2`, `delta.after == 3`, `expanded(delta)` — a fully deterministic `after > before` with no fuzzing/mutation/wall-clock involved (measurement, not campaign, is what's random-free here — `unionCoverageOf` just replays a fixed IR list through `inProcessTarget`). Second test asserts the anti-vacuity converse: an unchanged before/after pair must NOT report `expanded`. **TDD, RED confirmed for the real reason:** `coveragereport.nim` was moved aside, `t_coverage_report.nim` run first → `Error: cannot open file: ./coveragereport` (RED, the module genuinely didn't exist yet) → module restored → GREEN, both assertions passing on the first real attempt. **`-CorpusReport` verb** (`scripts/dev-test.ps1`, new `[switch]$CorpusReport` + optional `[string]$CorpusReportTarget`): calls `Invoke-NimContainer -NimFile "tests\coveragereport.nim" -NimArgs @('c','-r','--hints:off','--colors:off') -EnvVars $envVars`, deliberately DROPPING the suite loop's default `-d:chapulinTest` flag (this program needs none of api.nim's test-observable helpers) — a genuinely differently-shaped invocation exercising C0's `-NimArgs` passthrough, not the default args passed incidentally. Target selection threads through `-EnvVars` (`CHAPULIN_CORPUS_REPORT_TARGET`) rather than a program argv: confirmed against `nimcontainer.ps1` that `-NimArgs` splices only BEFORE the compiled file (`nim @NimArgs $NimFile`), so there is no seam for a post-file program argument — env var is this repo's already-established pattern (mirrors C1's `CHAPULIN_SOAK_SECONDS`), not a new one invented here. Defaults to the flagship real target `protocol.decode.soak-corpus` when no target is given. Kept structurally distinct from the suite loop (own `if`/`exit`, like `-Soak`), never touching the pass/fail-list contract. **Verified isolated** (`-Only @('t_coverage_report')`) and via **the verb itself, twice, bracketing a real `-Soak` campaign — the one-time §7 manual demonstration:** baseline `-CorpusReport` on the pre-existing committed corpus reported `committed entries: 9, visited edges: 13/8192`; ran `-Soak 5` (42248 iterations, corpus grown to 40 entries then capped to the `CorpusSizeCeiling=16` on save); re-ran `-CorpusReport` → `committed entries: 16, visited edges: 13/8192` — **edge count did NOT increase** (9→16 entries, 13→13 edges). This is not a bug: it is the RFC's own §3.3/§7-predicted outcome landing exactly as predicted — `protocol.decode` was already fuzzed to convergence on this coarse 8192-slot bitmap in v1/C1/C4, so a fresh short soak burst legitimately finds 0 new edges, which is precisely why the RFC mandates the automated RED-GREEN test be synthetic and forbids asserting `after > before` on this real target. Recorded honestly rather than re-run with a longer budget to manufacture a positive number. (Demo run left `tests/corpus/protocol.decode.soak-corpus.bin` at 16 entries, working-tree-only/uncommitted, same as every other corpus file this RFC has touched.) **Full default suite** (`pwsh scripts/dev-test.ps1`, no `-Only`, nothing else running) → 35 files, `All test files passed.` — confirmed `git status --short -- src/` shows only the pre-existing A4 three-file diff, nothing new. Not committed.
- C4 ✅ (2026-07-12) — minimization + size ceiling + provenance + C1-deferred replay-reassert, test-tooling only, no `src/`. New: `tests/corpusmin.nim` (`minimizeGrownCorpus`/`unionCoverageOf`), `tests/t_corpus_minimize.nim` (default), `tests/corpusminimize_decode.nim` (opt-in one-shot). **minimalCovering FINDING (RFC §3.3 corrected):** `minimalCovering()` is PRIVATE (no `*`) → only reachable via exported `FuzzSettings.minimizeCorpus`, which computes Coverage only for entries discovered IN THAT RUN (preloaded seeds get zero Coverage from captureIR) → "reduce fresh discoveries" not "losslessly reduce a curated file." Resolution: continuation-minimization via the exported door (deterministic `maxIterations`, never timeBudget) + independent coverage re-verify via `inProcessTarget`/`Observation.coverage`. Result: `protocol.decode.soak-corpus` 16→9, coverage held 13/13. Both privacy findings → proptest ticket (Corey repo). **Size ceiling:** `CorpusSizeCeiling*=16` in fuzzsupport.nim (names C1's existing maxEntries=16 once; soakrunner/interopcapture reference it); default test asserts every `.soak-corpus`/`.soak-crash` primary count ≤ ceiling. **Provenance:** primary section has no metadata slot → reuse SECONDARY section (`ScoredEntry.scores: Table[string,float]`, exported, unused by these testIds) via `tagProvenance`/`loadProvenance` (`source: CampaignSource` enum + `campaignEpochDay` ALWAYS caller-supplied CLI arg — NO clock call). **C1-DEFERRED replay-reassert NOW BUILT:** t_corpus_minimize suite 4 reloads soak-corpus/.soak-crash + regenerated C2 dictionary, re-asserts each replays cleanly via `byteSeqs()`+`decodeProp` (exported from soak_decode.nim); interop-capture honestly vacuous (0 entries, matches C3 Windows gap). RED→GREEN via synthetic 5-branch target. Minimized `tests/corpus/protocol.decode.soak-corpus.bin` regenerated 16→9 (working-tree only). Full default suite green (34 files), serial.
- C3 ✅ (2026-07-12) — interop capture point (prereq) + harvest, test-infra + test-tooling only, **no `src/` change** (confirmed via `git diff --name-only -- src/` before finishing — only pre-existing A4 src/ diffs present, nothing new). **Capture mechanism (the prereq):** `docker-compose.yml` — two new tcpdump-sidecar services, `pcap-tftp-server` (`network_mode: "service:tftp-server"`, captures Direction-1 traffic — our client vs the third-party `pghalliday/tftp` server — over a real Ethernet link, pcap linktype 1) and `pcap-our-server` (`network_mode: "service:our-server"`, captures Direction-2 traffic — atftp vs our own server, INCLUDING the always-PASS-tainted PUT leg — over loopback since atftp already shares our-server's network namespace, pcap linktype 0/DLT_NULL); both run a plain `udp` capture filter (no port clause — see finding below) via a new `Dockerfile.pcap` (`alpine:3.20` + `apk add tcpdump`, official packages only, no third-party image tag) and bind-mount `./interop-captures/` (gitignored — runtime artifact, not source) for the operator to retrieve `.pcap` output. **Confirmed the RFC's own premise first:** `docker-compose.yml` had zero pcap/tcpdump capture before this slice — the RFC's "no capture exists" claim held exactly. **Harvest:** new `tests/interopcapture.nim` — a from-scratch classic-pcap (not pcapng) parser (`extractUdpPayloads*`, handles linktype 0/Null-loopback, 1/Ethernet, and 113/Linux-SLL) + `harvestPcapBytes*`/`harvestPcapFile*` depositing every extracted UDP payload through C2's `encodeByteSeqIR` (`./soakseeds.nim`, imported not re-derived) into the same `tests/corpus` `directoryBasedDatabase` convention, under `interopCaptureTaggedId(base)` (`<base>.interop-capture`) / `interopCaptureUnverifiedTaggedId(base)` (`<base>.interop-capture-unverified`) — testId suffixes mirroring `soakrunner.nim`'s `.soak-corpus`/`.soak-crash` convention (C1's own lesson: a distinct testId no `fuzzProperty` call references is never pruned by `dbReusePhase`). **Two findings made building the parser (applied, not just noted):** (1) NO port filter — TFTP's reply comes from a per-transfer ephemeral port, not the well-known port the request targeted, so a port-filtered parser would capture only each transfer's first packet and silently drop every DATA/ACK/ERROR after it; both the parser and the tcpdump filter itself (`udp`, no port clause) reflect this. (2) Payload length is sliced from the UDP header's OWN 16-bit length field, not the pcap record's captured length — a short TFTP packet captured over a real Ethernet link can carry NIC-inserted padding to the 46-byte minimum frame size that the UDP length field doesn't count; slicing by captured length would hand `encodeByteSeqIR` a seed with junk trailing zero bytes. **Always-PASS caveat handling:** `interopCaptureUnverifiedTaggedId` is the encoded/documented answer to the task's caveat requirement — seeds a real harvest would pull from atftp's PUT leg (`docker-compose.yml`'s `echo "PASS: PUT completed (exit $$?)"`, confirmed by direct read to report `echo`'s own exit code, not atftp's, i.e. it ALWAYS prints PASS) land under the `-unverified` suffix, never merged with the verified-leg corpus. **TDD, RED confirmed for a REAL reason (not staged):** the harvest test's first cut asserted `reloaded[i] == originals[i]` positionally after a 3-packet harvest and failed with an exact index-0/index-2 reversal — traced to `proptest`'s own `db.nim` `applySave`: `c.primary = @[choices] & deduped` PREPENDS each save, so `loadPrimary` returns most-recently-saved-first, not insertion order (a real fact about the shared corpus mechanism, not a chapulin bug). Fixed by matching per-entry by CONTENT (`generated in originals`) plus a per-opcode field-level assertion and an all-three-matched-exactly-once tripwire, rather than assuming order — GREEN after. **Honest scope-down (this environment specifically, not a blocker):** live capture could not be exercised here — `docker info --format '{{.OSType}}'` reports `windows` (this Docker Desktop is pinned to Windows containers), and `docker pull pghalliday/tftp`/`docker pull alpine` both fail outright with `image operating system "linux" cannot be used on this platform`; the interop stack (and the new pcap sidecars, and Dockerfile.pcap) are Linux-only by construction and cannot even be attempted here. Did NOT attempt to flip Docker Desktop's engine mode to Linux containers to work around this — that's a global host config change outside this slice's blast radius, not reliably reversible within one session, and not something to do unannounced to Corey's environment. So: the mechanism is built and `docker compose config` confirms it parses; the harvest path is proven end-to-end against a REPRESENTATIVE in-memory sample instead of a genuine capture (real `protocol.encode`-built TFTP wire bytes — RRQ w/ option, DATA, ACK, ERROR, WRQ — wrapped in hand-built-but-standards-conformant pcap/Ethernet-or-Loopback/IPv4/UDP framing, including a deliberately-padded Ethernet frame and a truncated trailing record to exercise both findings above directly) — never dressed up as "a capture happened." New test `tests/t_interop_capture.nim` (registered in `scripts/dev-test.ps1`'s default `$tests`, same judgment call C2's `t_soak_encoder` already recorded — fast, deterministic, no Docker/z3 needed) uses a DISPOSABLE tmp `directoryBasedDatabase` throughout (never touches the real `tests/corpus/`) specifically so no synthetic packet is ever deposited into the committed corpus under an `interop-capture` provenance tag that would misrepresent it as a real one. **Manual/opt-in real-harvest path (documented, not run here):** once the interop stack is brought up somewhere with Linux containers (a CI runner, WSL2 Docker Desktop switched to Linux containers, a Linux host), `docker-compose.yml`'s own new comment block gives the exact `docker compose up` invocations and the `nim c -r tests/interopcapture.nim <pcap-path> <testId-base> [unverified]` harvest command (`interopcapture.nim`'s `when isMainModule`) — inherently manual, the same way C1's `-Soak` mode is, so intentionally NOT part of the default suite. Verified GREEN in isolation (`-Only @('t_interop_capture')`) and the full 32-file default suite (`pwsh scripts/dev-test.ps1`, no `-Only`) — both clean serial runs, nothing else running concurrently: `All test files passed.` Not committed.
- C2 ✅ (2026-07-12) — byte→choice-IR seed encoder + round-trip-through-the-ACTUAL-strategy test + boundary/known-bad/reserved-name dictionary, test-tooling only, no `src/`. New files: `tests/soakseeds.nim` (`encodeByteSeqIR*(bytes): seq[ChoiceNode]` — the reusable encoder, C3's harvest is meant to call this same proc — plus the seed dictionary: `BoundaryValueStrings`/`KnownBadOptionStrings`/`ReservedDeviceNames` consts and `mkOptionPacket`/`mkFilenamePacket`/`mkDanglingKeyPacket`/`boundaryOptionSeeds`/`knownBadOptionSeeds`/`reservedNameSeeds`/`allDictionarySeeds` builders, all wire bytes built via the REAL `protocol.encode`/`TftpPacket` except the one deliberately-malformed dangling-option-key case, which hand-writes raw bytes since `protocol.encode` structurally cannot produce it), `tests/t_soak_encoder.nim` (registered in `scripts/dev-test.ps1`'s default `$tests`). **Choice-IR protocol CONFIRMED against source** (not merely cited from the RFC): `_deps/proptest/src/proptest/strategy.nim:451-472`'s `lists`/`integers(0,255)` draws element-at-a-time — a `ckBoolean` continuation gate (`drawBoolean(p)`) precedes EVERY element, then the element's own `ckInteger`, terminated by a single trailing `ckBoolean(false)` — exactly the RFC's "continuation-boolean protocol," 2N+1 nodes for N bytes. `datasource.nim`'s `takeReplay(kind)` (~120-127) raises `Overrun` the instant the next recorded node's `kind` doesn't match what the strategy is about to draw next — this is the mechanism behind "malformed IR seeds are silently dropped" (`fuzz.nim`'s `captureIR`, ~262-277, catches `Rejection`/`Overrun`/any `CatchableError`/`Defect` from `s.generate(newReplaySource(choices))` and returns `(ok: false, ...)`, and `fuzz()`'s `initialIRCorpus` loop, ~381-383, only appends entries with `cap.ok`). **TDD, RED confirmed for the right reason:** the FIRST `encodeByteSeqIR` cut was deliberately naive (one bare `ckInteger` per byte, no continuation booleans — the "a list of bytes is a list of int choices" mistake one would make without reading `strategy.nim`) — run against Docker, it failed EVERY round-trip test with `Unhandled exception: choice kind mismatch during replay [Overrun]` (not a silent wrong-value failure, but the "seed dropped" failure mode `captureIR` documents) — then replaced with the interleaved-boolean version, GREEN on the next run. **THE CRITICAL TEST:** every dictionary entry's round-trip assertion calls `fuzzsupport.byteSeqs().generate(newReplaySource(encodeByteSeqIR(intendedBytes)))` (the literal soak-target strategy `soak_decode.nim` fuzzes) and asserts (1) `generated == intendedBytes` byte-exact AND (2) `protocol.decode(generated)` extracts the SAME semantic boundary value that was encoded (the option value string for blksize/tsize/windowsize boundaries and known-bad strings; the filename for reserved device names; a `TftpDecodeError` for the structurally-malformed dangling-key case) — proving the seed produces the intended boundary through the real decoder, not just a structural IR round-trip. **Dictionary categories:** boundary values (blksize/tsize/windowsize × {0, -1, 2^16±1/0, 2^32±1/0, 2^63±1/0, int64-min}) as RRQ option values; known-bad option strings (empty, non-numeric, whitespace, hex, fractional, signed, NaN, bare-sign, oversized-digit-string) × the same 3 option keys, plus one structurally-dangling-key packet; reserved Windows device names (CON/PRN/AUX/NUL/COM1-9/LPT1-9 + case-insensitive + dotted-extension variants) as RRQ filenames — all via `protocol.encode`/hand-built raw bytes, never re-deriving the wire format for the well-formed cases. **Suite placement (justified):** registered in the default `$tests` array, not opt-in — it's fast (no coverage-guided search, no z3, purely deterministic construction+replay) and behaviorally identical in shape to every other cheap unit suite already in that list (t_protocol, t_options, ...); opting it out would be the outlier decision, not the other way around. **Object-variant gotcha found+fixed:** an initial `mkOptionPacket(..., opcode = opRrq)` (parameterizing over opcode with a default) hit Nim's "cannot prove that it's safe to initialize ... with the runtime value for the discriminator" — `TftpPacket` is an object variant and a *runtime* opcode parameter can't feed the discriminator directly; fixed by hardcoding `opRrq` in both builders (this slice never needed WRQ/OACK shapes) rather than fighting the variant-construction restriction. No new toolchain blocker beyond that (mechanical, not proptest-side). Verified GREEN in isolation (`-Only @('t_soak_encoder')`) and the full 31-file default suite (`pwsh scripts/dev-test.ps1`, no `-Only`) — both clean serial runs, nothing else running concurrently. Not committed.
- C1 ✅ (2026-07-12) — `fuzzWith` `fmIR` soak scaffold, test-tooling only, no `src/`. New files: `tests/soakrunner.nim` (shared `runSoak[T]` fuzzWith/fmIR driver, reusable by C2+), `tests/soak_decode.nim` (real `protocol.decode` soak target — chosen over A3's StateMachine because `fuzzWith` takes a plain `Strategy[T]`+`proc(x)`, and t_props' decode target already has the `byteSeqs()` strategy+oracle shape), `tests/soak_canary.nim` (synthetic crasher proving the first-Defect path). No `.nim.cfg` (base image; `tests/nim.cfg` supplies `-d:chapulinFuzz`); none in the default `$tests` list. **`-Soak <int>` mode:** new param on dev-test.ps1; when `>0`, a block BEFORE the suite loop runs `Invoke-NimContainer -NimFile soak_decode.nim -EnvVars @{CHAPULIN_SOAK_SECONDS=$Soak}` and exits with its code — never touches the suite pass/fail contract; absent → default suite identical. `getEnv→FuzzSettings.timeBudget` (no recompile). **Verified isolated:** (a) `-Soak 8` → budget=8s, 58491 iters (vs `-Soak 3`→23640, scales → timeBudget honored); (b) canary Defect caught→shrunk→committed `tests/corpus/soak.canary.soak-crash.bin`→exit 1; (c) clean decode soak deposited 44 IR entries to `tests/corpus/protocol.decode.soak-corpus.bin` via directoryBasedDatabase, resumes as initialIRCorpus. **DESIGN DECISION (recommendable, applied):** proptest's `dbReusePhase` batch-`removeMany`s any PRIMARY entry that doesn't currently falsify → naively growing the same testId a `fuzzProperty` target uses self-erases on the next default run. Fixed by persisting clean growth under distinct `.soak-corpus` suffix + crashes under `.soak-crash` (sibling testIds in the same tests/corpus root, nothing prunes them); `protocol.decode.bin` confirmed byte-identical to HEAD after a soak. Updated `tests/t_corpus.nim` to recognize the new convention (primary-populated), green. **NEW toolchain blocker (proptest, Corey-owned — needs ticket):** `shrink(s,prop,choices)` 2-arg overload fails to COMPILE for `T=seq[byte]` from a chapulin test module (`shrinker.nim:274 type mismatch Int128 </dist` open-symbol resolution gap in `shrink`→`defaultShrinkPasses`→`lowerIntegerShrinkPass`; compiles fine for `T=int`) → worked around with the 5-arg overload + explicit `@[deleteSpansShrinkPass[T]()]`. Cost: byte-list crash minimization shortens (span/element delete) but doesn't lower surviving byte values toward 0. **Automated default-suite replay-and-re-assert of the soak corpus deliberately DEFERRED to C4** (its RFC home), not invented ad hoc here.
- C0 ✅ (2026-07-12) — `scripts/lib/nimcontainer.ps1` (`Invoke-NimContainer`: ProjectRoot/NimFile/Image/**EnvVars**(C1 -Soak)/**NimArgs**(C5 -CorpusReport); milpa-fetch memoized per-process; image precedence `-Image > t_symex* > base` preserved; returns exit code, pass/fail accounting stays in dev-test.ps1). dev-test.ps1 sources it + calls per-suite. **BUG shipped then FIXED (found by C1's full-suite run — a REAL bug, not the concurrency I first guessed):** the bare `docker run` statement leaked container stdout into the function's return value → `$exitCode` captured an `Object[]` → every `-ne 0` check always truthy → ALL suites misreported FAILED. Fixed: `docker run ... | Out-Host` + explicit `return $LASTEXITCODE`. Full default suite (30) now clean-serial GREEN.
- **INFRA FIX applied (2026-07-12, control loop):** `.gitignore` — added `!tests/t_*.nim.cfg` negation. The existing `tests/t_*` + `!tests/t_*.nim` pair was ignoring EVERY `.nim.cfg`, so the symex suites' z3-include-path sidecars (v1's `t_symex.nim.cfg`/`t_symex_smoke.nim.cfg` AND v2's `t_symex_decode/security/uri/checksum/netascii.nim.cfg`) were all un-committable → symex suites would silently break on a fresh clone. Now trackable. **These 7 `.cfg` files must be staged when this work is committed.**
- B3 ✅ (2026-07-12) — `tests/t_symex_checksum.nim` + `tests/t_symex_netascii.nim` (+ their `.nim.cfg`; opt-in z3, not in default list). **Target 1 (`checksum.writeSidecar` lexical):** the RFC's own hypothesis held exactly — the sidecar-path derivation (`checksum.nim:80`: `resolvedPath & SidecarExt`) is a single `&`-concat with no loop and no branch, so `sidecarPathTwin` proves `sxUnsat` for tIndexError+tFieldDefect **directly, no twin-extraction gymnastics needed** (the shipped `writeSidecar` proc itself still can't be symex'd — same unmodeled-syscall wall as B2 Target 1 — but the LEXICAL PREFIX this slice scopes to needed none of that). Differential oracle is INDIRECT but exact: since the real `sidecar` local isn't returned, it drives the real `writeSidecar` against a disposable temp root and asserts `fileExists` at exactly the twin's predicted path (hand vectors + a `benignNameStrings` property, `FuzzSeed`/`FuzzN`) — a divergence in either derivation surfaces as a missing/wrong-path file. **Target 2 (`netascii.NetasciiDecoder.feed`/`flush` non-conformant-CR branch):** per the RFC's own design constraint, modeled **per-transition, loop-free from the start** (`netasciiDecodeTransitionTwin(pendingCr: bool, rawByte: int)`, B1's `atByte` masked-int chokepoint reused for the scalar byte) rather than looping over a buffer — sidesteps B2's Blocker #9 entirely (that target's `feed`-body frame has no `string` param at all, only a plain `bool` field, so #9 may not even apply here, but going loop-free was the instructed design from the start, not an empirical rediscovery) and is *stronger* than a bounded loop-unwind proof: it's EXHAUSTIVE over the full 512-state (`pendingCr` × byte) space, not bounded by `maxLoopUnwind`. `sxUnsat` for tIndexError+tFieldDefect (trivially — the transition has no indexing/no variant-object field at all once loop-freed, so this is a genuine-but-low-drama proof, not a near-miss). Differential oracle drives the REAL `NetasciiDecoder.feed` (priming with a lone CR to reach `pendingCr==true`, matching the decoder's own documented defer contract) through **all 512 (state, byte) pairs exhaustively** — not sampled — plus hand vectors for the exact hostile-arm shapes (`CR,X`; `CR,CR,LF`; `CR,CR,NUL`) and a tie-back suite driving the real multi-byte `feed()` directly on those same sequences. **Citation drift found (documented, not a blocker):** the RFC/handoff cite `netascii.nim ~120-123` for the non-conformant-CR branch; that's actually `NetasciiDecoder.flush` (trailing-lone-CR-at-EOF). The branch this slice targets (`feed`'s in-stream non-conformant-CR arm) is at **netascii.nim:110-113** — content matches the RFC's description exactly (module doc's own R2 section + the cited prior round-2 false-EOF bug), just a different line number; mechanical drift only (same class as A4's citation-drift note), no escalation. No new toolchain blockers found for the catalog (both targets landed clean on the first attempt — no RED phase beyond initial import-cleanup). No `src/` changes. Full symex-opt-in runs (`-Only @('t_symex_checksum')` / `-Only @('t_symex_netascii')`) both GREEN; confirmed absent from `dev-test.ps1`'s default `$tests` list.
- B2 ✅ (2026-07-12) — `tests/t_symex_security.nim` + `tests/t_symex_uri.nim` (+ their `.nim.cfg`; opt-in z3, not in default list). **Target 1 (validatePath/checkWriteAccess lexical):** pre-I/O twins extracted (`validatePathLexicalTwin` = guard chain through first `isWithin`, `absolutePath`≈string-concat since served root is always absolute; `isReservedSidecarNameSymexTwin` = pre-canonicalize `.md5` check) — shipped procs correctly NOT attempted (canonicalize/symlinkExists/hasReparseComponent absent from OpaqueEffectfulProcs). `sxUnsat` for tIndexError+tFieldDefect on both; differential oracles (hand + 2 fuzzers) green with 2 divergences ASSERTED explicitly not hidden (case-folding #7; `os.extractFilename("//a/..")==""` UNC quirk). **Target 2 (parseTftpUri/isTftpUri):** shipped procs → `sxUnknown` (toLowerAscii #7, rfind #8 unmodeled) → extracted `parseTftpUriTwin` (case-sensitive checks, forward `find` for `rfind`); `sxUnsat` for tIndexError+tFieldDefect+tRaisedExn(ValueError); differential oracle green (host/port tokens colon-free to keep find≡rfind exact). No `src/` changes.
- **BLOCKER CATALOG — READ BEFORE ANY symex slice (B1 #1-6, B2 #7-11):** (#1) seq[byte] no witness → seq[int] masked 0..255. (#2) tuple return unsupported → `var` out-param. (#3) bitwise ops on plain-int symbolic crash walker → arithmetic equiv (`((x mod 256)+256) mod 256`, `hi*256+lo`), mask every read via one chokepoint. (#4) implicit bare-tail-expr return of a local crashes → explicit `result =`/`return`. (#5) var-out-param call inside a loop crashes natively → hand-unroll. (#6) chained bounded scans (2nd offset = 1st symbolic result) → sxUnknown (dependent-loop gap). (#7) `toLowerAscii`/`toUpperAscii` unmodeled → sxUnknown → use case-SENSITIVE checks in twin. (#8) `rfind` unmodeled (only `find` is) → forward `find` in twin. (#9) **ANY while/for loop in a symex target whose frame ALSO has a `string` param → sxUnknown (empty errors), regardless of what the loop does** — distinct from #6; bisected 8 variants. Single non-loop string index is fine. (#10) `string.add` (in-place) unsupported (`iekStrUnsupported`) → use `&`-concat-and-reassign. (#11) helper with 3-level nested-`if` over a string whose result the caller then applies another string op to → **HARD NATIVE CRASH** (not sxUnknown); bisected to the 2-vs-3 depth threshold → bound string twins to ≤2-deep. **Also:** `symexAssume` parses to `mkAssert` (== symexAssert), not the documented early-return filter → proptest ticket (Corey-owned repo).
- B1 ✅ (2026-07-12) — `tests/t_symex_decode.nim` (opt-in z3 image `chapulin-symex:2.2.10` via t_symex* convention; NOT in the default suite list — confirmed). Verified green by control loop: fixed-size arms (opData/opAck/opError) `sxUnsat` for tIndexError+tFieldDefect; option arm `sxUnsat` for **header dispatch + first filename scan only** (see below); differential oracle (`decodeIntTwin(seq[int])` ≡ real `decode(seq[byte])`) over hand vectors + fuzzed `byteSeqs`. **6 proptest-symex toolchain blockers found empirically — CATALOG FOR B2/B3 (they reuse the seq[int]-masked twin, will hit these):** (#1) `seq[byte]` has no symex witness reader → `seq[int]` masked to 0..255 (RFC-anticipated). (#2) tuple-constructor return (`return (s,i+1)`) unsupported by dsl_parser → reshape to `var` out-param (mechanical, recommendable fork; `readCString`-shaped helpers must do this). (#3) **bitwise ops (`and`/`shl`/`or`) on a plain-`int` symbolic value crash the walker** (runtime.nim doAssert — plain int has no fixed width to promote to bitvector) → use ARITHMETIC equivalents (`((x mod 256)+256) mod 256` for `and 0xFF`; `hi*256+lo` for `(hi shl 8) or lo`); mask at EVERY element read via one chokepoint. (#4) implicit bare-tail-expression return referencing a local crashes (KeyError) → use explicit `result =` or `return <expr>`. (#5) calling a var-out-param helper INSIDE a for/while loop crashes the compiled walker natively (no message) → hand-unroll, no loop around such calls. (#6) **chained bounded scans (2nd scan's start offset = 1st scan's symbolic result) → `sxUnknown`**, invariant under maxLoopUnwind/maxCallDepth/symexAssume — genuine dependent-loop solver gap; this is why the option arm scopes to header+first-scan, NOT the RFC's original ≤2-option target (RFC §3.2/§7 corrected). **Also:** `symexAssume` parses to `mkAssert` (identical to `symexAssert`), not the documented early-return filter → **needs a proptest ticket** (Corey-owned repo). No `src/` changes.
- A4 ✅ (2026-07-12) — **the ONLY prod-code slice in this RFC, now shipped.** **(prod, src/ — exactly 3 files):** `server.nim` -- rerouted all three reject sites (no-available-port in `handleRequest` at what is now line 750ish; host-access denial and maxConcurrent in `run()`, ~904/919) from `newUdpTransport(0)` to `server.transferFactory(0)` (each keeps its own `try/except OSError, CatchableError`, modeled on `allocateTransferTransport`), + new `ServerCallbacks.onRejected*: proc(clientHost, clientPort, code: TftpErrorCode, msg: string) {.closure.}` (optional/nil-safe — every call site is `if server.callbacks.onRejected != nil: ...`, so a nil hook is zero behavior change); `eventqueue.nim` -- new `EventKind.evServerRejected` + `Event` case branch (`rejClientHost/rejClientPort/rejCode/rejMsg`) — `xfrId` stays `NoTransfer` since no reqId/TransferId exists yet at reject time; `api.nim` -- `startServer`'s `ServerCallbacks` construction wires `onRejected` into `cS.enqueue(Event(kind: evServerRejected, ...))`, same shape/style as the existing 4 hooks. Line-drift note: r1/r2's cited `server.nim:701/852/862` are now ~750/904/919 after A1a-i/A1a-ii/A1b/A2/A3's accumulated test-only churn elsewhere in the file's surrounding context (server.nim itself hadn't been touched until this slice) — mechanical drift only, no structural surprise, no escalation needed. **(test, over Wire):** new `tests/t_a4_maxconcurrent.nim` (registered), two suites: (1) **direct `TftpServer`/`run()`** (bypasses api.nim on purpose — the session facade has no getter for the underlying `TftpServer`, so this is the only way to read `server.activeTransfers` directly): `WireRegistry`-backed `transferFactory`, hand-fed `ListenerQueue` with 2 accepted RRQs (maxConcurrent=2, each blocks mid-transfer awaiting an ACK nothing ever sends, so `activeTransfers` stays elevated for the test's whole bounded pump) + a 3rd that must reject; `onRejected` closure reads `srv.activeTransfers` synchronously (single-threaded, no race) — asserts **`activeAtReject == cfg.maxConcurrent`, the literal RFC anti-vacuity condition**, NOT `activeClientCount` (disjoint, outbound-only, never touched by this all-inbound scenario). (2) **api.nim session facade** (`newSession`+`startServer`, maxConcurrent=1): asserts the rejection surfaces as a structured `evServerRejected` Event (`xfrId == NoTransfer`, `rejClientHost/rejClientPort/rejCode` match the rejected peer, `rejMsg` non-empty) — proving the embedding-facing (#17) half of the deliverable, not a free-text `evServerLog` substring. **Zero-real-socket:** structural, not counter-based — every transport in both suites comes from the `WireRegistry` (never touches `transport.newUdpTransport`); `reg.wires.len == 3` in suite 1 (2 accepted + 1 reject-site mint, now that the reject sites share the seam) confirms every mint was accounted for. **Escalation considered and declined:** RFC §7 asks for "a test-only counter in the default `newUdpTransport` factory" if adding one is clean — declined as OUT OF SCOPE: `transport.nim` is not one of the three files the task scoped as "the ONLY src/ changes in this whole RFC," and the WireRegistry-based test already makes real-socket-freedom structural (transferFactory literally never calls `newUdpTransport`), so the counter would add a 4th src/ file for no incremental proof. Noted here per the fork-test's "recommendable ⇒ do it and note it" instruction. **TDD:** RED confirmed for the right reason first — `git stash`ed just the 3 prod files, re-ran the new suite, got a real compile error (`undeclared field: 'onRejected' for type server.ServerCallbacks`), then restored and reran GREEN. No refactor round needed (first implementation was clean). **Full suite:** `pwsh scripts/dev-test.ps1` (all 30 files, no `-Only`) → `All test files passed.` — existing accepted-request behavior (evTransferStarted etc.) unaffected, confirmed by suite 2's own `startedEvents.len == 1` assertion. Not committed.
- A3 ✅ (2026-07-12) — test-only extension of `tests/t_a2_facade_stateful.nim` (second `StateMachine[A2State]` `a3SM` reusing A2 scaffolding) + additive `invert: bool=false` param on all 8 injectrules builders (default no-op, t_hostile stays green; needed because A3's per-transfer Wire has sideA=server (WireRegistry.factory) vs t_hostile's sideA=client). **18 injection rules:** all 8 same-TID + 8 off-TID injectrules families (invert=true, live-wire preconditioned) + 2 bespoke rules (malformed RRQ/WRQ to listener; forged OACK to client). **Coverage-guidance ON** via hand-written `a3Settings()` (`coverageGuided:true, testId:"facade.hostileInjection", dbPath:CorpusDir, seed:FuzzSeed`) — corpus `tests/corpus/facade.hostileInjection.bin` persists. **FINDING (bounded, applied):** proptest targeting hill-climb OOM'd at fuzzProperty's literal maxExamples=200/maxSteps=20 (re-runs the whole two-real-session property per integer choice × log-scaled deltas; injectrules per-byte payloads make one garbage injection combinatorially dominate). Empirically probed (steps 4-7 ≈16-23s; **step 8 = 30+min OOM-kill**) → bounded to `maxExamples=20, maxSteps=6` (~16s), coverage+corpus still ON per RFC; deep campaign is Part C's job, not A3's default run. **FLAG for Stage 4:** the 2 bespoke rules are plain `rule()` over `var A2State` → "touch only wire+counter" is by-convention there, NOT compiler-enforced like the 16 injectrules-routed families (which use the non-var NotifyInject signature). **Both-sides liveness:** `a3ClientProgressed`/`a3ServerProgressed` independently asserted to fire (facade analogue of "check BOTH futures"). No-Defect invariant preserved (never-catch → propagates → canary-proven falsification). Full suite green ~4m49s.
- A2 ✅ (2026-07-12) — `tests/t_a2_facade_stateful.nim` (registered) + additive `pumpSessionCollect` in `wireharness.nim`. proptest stateful `StateMachine[A2State]` over a REAL client↔server `TftpSession` pair (A1b wiring). **Op vocab:** startTransfer(get/put), startServer, cancel (Bundle over live transfer-id pool), stop, single shared pump op, close; startTransfer strategy varies mode(octet/netascii)×blocksize{8,512,65464}×windowsize{1,4,65535} with `\n` straddling 8-byte block boundaries. **Invariants:** (1) no Defect escapes api.nim except-CatchableError boundaries (proven by NOT catching → propagates & falsifies, canary-proven pipeline); (2) poll never raises (poll(0) every rule step); (3) drain/close terminate within a fixed **100-round (600×poll(0)) tick cap** — sized from the mock's spin-recv chain (~84 rounds/6-tick, chains drain concurrently on the one global dispatcher so bound=longest chain not sum), never wall-clock drain(). waitTransfer/waitServer in a SEPARATE deterministic suite (never-raise + buffered-event survival). Anti-vacuity: module VacuityCounters assert every op ran live + pump advanced real transfers + variety drawn; vacuous run fails. **DECISION (recommendable deviation, applied):** A2 uses a bounded deterministic `property` (`Settings(coverageGuided:false, seed:FuzzSeed, maxExamples:FuzzN)`) reusing fuzzsupport VacuityCounter/generators/FuzzSeed/FuzzN — NOT `fuzzProperty` — because coverage-guidance with no injection dimension explored blindly (16-19+ min, ~10× cost) for zero benefit. RFC §5 scopes coverage-guided+corpus to **A3** (line 91), not A2 (line 90) — so A3 must turn `fuzzProperty` coverage-guidance ON. Runs <1 min. Full suite green.
- **Slice progress:** A1a-i ✅ (2026-07-11) — `WireRegistry` in `tests/wireharness.nim` (`newWireRegistry`/`factory` → `TransportFactory`; call-order index `wires.len` at mint, NOT port key, sidesteps the `transferFactory(0)` degeneracy; `swallowFirst` fwd-compat passthrough for A1a-ii). `tests/t_wireregistry.nim` green. NOTE: `transferFactory(0)` degeneracy is at `server.nim:710` (not `:674` as r1/r2 cite — line drift only). / A-shared ✅ (2026-07-11) — `tests/injectrules.nim` (both same-TID + off-TID families over `(wireOf, notify)`, notify non-var), `t_hostile.nim` retrofitted+green, `pumpSession*(s, ticks=6)` in wireharness.nim, `t_asynccheck_tripwire.nim` (call-form scan, 0 hits), `t_defect_canary.nim` (two-path: evalReplay→`"strategy crashed:"` + cold forAll→`"crashed:"`). RFC §3.1/§7 corrected: the `"strategy crashed:"` claim holds on the REPLAY path only; cold discovery reports coarse `"crashed:"`; the always-present guarantee is substring `"crashed:"` (A2/A3 hit replay via fuzzProperty persisted testId). Full 26-file suite green. Not committed.
- A1a-ii ✅ (2026-07-11) — **piece (b)** `makeListenerFromWire*(w: Wire, port = 0): UdpListener` (`tests/wireharness.nim`) drains `w.a2b` directly (mirrors `makeListener`'s shape, backed by a Wire instead of a `ListenerQueue`) so a client Transport's REAL first send (`makeTransport(w, sideA = true)`, no `swallowFirst`) reaches it — no hand-fed queue. **piece (c) — the novel one** — `makeAdoptingTransport*(listenerWire: Wire, reg: WireRegistry): Transport`: starts unadopted (sends go to `listenerWire`, the well-known "port"); its first `recv` spin-waits until `reg.wires.len` grows past the construction-time baseline (i.e. until the server's `allocateTransferTransport`/`transferFactory` mints a per-transfer Wire in response to THIS request), then permanently adopts `makeTransport(reg.wires[baseline], sideA = false)` for every later send/recv — a one-shot lock mirroring `transfer.nim`'s own `PeerEndpoint.lockTo`. **Key finding, source-verified:** `Wire.wireSend` tags every legit packet with the SAME constant source identity (`WirePeerHost`/`WirePeerPort`) regardless of which Wire object carried it — there is no numeric port in the packet payload that differs per Wire, so literal "learn the port from the packet's port field" would be theater in this mock. The registry's per-call-order Wire IS the per-transfer port in this harness (A4's registry is the same fact, §3.1) — watching `reg.wires` grow is the faithful mechanism, not a side-channel workaround; no changes to `Wire`'s core send-tagging were needed or made. `tests/t_listenerbridge.nim` (2 tests: bridge delivery of a real RRQ; full adoption — server mints Wire, replies bare DATA(1), client's ACK observably lands on `reg.wires[0]` not `listenerWire`, transfer completes with `evTransferComplete`/3 bytes) — green. Registered in `scripts/dev-test.ps1`. Full 27-file default suite green (`t_session`/`t_props_server`/`t_wireregistry`/`t_hostile` included). No `src/` changes (test-only, per slice scope). Not committed.
- A1b ✅ (2026-07-11) — get/put smoke, test-only, no `src/` changes. `tests/t_a1b_smoke.nim` (registered in `scripts/dev-test.ps1`): a real client `TftpSession` completes BOTH a GET and a PUT over ONE `Wire` against a REAL **server** `TftpSession` (`startServer` — its actual accept loop + `ServerCallbacks`, not a hand-simulated "serverSide" proc), file I/O on both sides via the BlockSource/BlockSink in-memory adapters (`memoryBlockSource`/`memoryBlockSink` through `ServerConfig.sourceFactory`/`sinkFactory` server-side, `TftpSession.sourceFactory`/`sinkFactory` client-side). Wiring: server session `transportFactory = reg.factory()` (A1a-i) + `listenerFactory = makeListenerFromWire(listenerWire)` (A1a-ii b); client session `transportFactory = makeAdoptingTransport(listenerWire, reg)` (A1a-ii c) — i.e. exactly the RFC-mandated A1a primitives, injected through `newSession`'s existing seams, no new plumbing. `srv.transferFactory` (server.nim) is itself wired from the server session's own `transportFactory` (`api.nim startServer`: `cS.transportFactory("127.0.0.1", port)`), so `reg.factory()` mints the per-transfer Wire the same way `allocateTransferTransport` does in production. 2 tests, both multi-block (1400B/1600B, DefaultBlocksize 512 → short final block): GET and PUT, each asserting byte-exact round-trip AND `not fileExists(...)` for the path that would have been touched under the file-backed default on BOTH client and server — reaches the RRQ/WRQ negotiation phase end-to-end (closes v1's R1-7 gap), zero temp-dir data I/O. **Reconciled with `t_blocksource_demo.nim` (BlockSource/BlockSink RFC slice 5/"6"):** that suite already proved the BlockSource/BlockSink adapters compose hermetically through the facade, but by DESIGN drives `handleRrq`/`handleWrq` directly (its own RFC's R2-6 explicitly descoped the listener bridge as "not yet built" at the time) — no `startServer`, no accept loop, no `TftpServer`/`ServerCallbacks` in the loop at all. A1b is therefore genuinely additive, not a duplicate: it is the first test in the repo where BOTH parties are real `TftpSession`s talking through the full facade (client `startTransfer` → real listener accept → real per-transfer transport allocation → real `ServerCallbacks`→event translation) with memory-backed I/O, closing the exact gap `t_blocksource_demo.nim`'s own RFC left open for v2 to fill. Recommendation followed: build A1b on the A1a-ii bridge (per RFC), not a copy of the demo's direct-handler-call shape — the demo's approach cannot exercise the accept-loop/listener/event-translation code A1b's invariants (and later A2/A3/A4) actually need. No spec drift found; no `src/` changes; no blocker. First run GREEN (no defect-fixing round needed — this slice composes already-individually-proven primitives, so there was no substantive RED phase beyond initial typos). Full 28-file default suite green (`pwsh scripts/dev-test.ps1` → "All test files passed."). Not committed.
- **BlockSource/BlockSink RFC:** `docs/rfc/in-memory-sources-sinks.md` — **Stage 3 COMPLETE (implemented, green)**; see its handoff `docs/rfc/in-memory-sources-sinks.handoff.md` (heading into its own Stage 4 `/code-review`). **NOT GitHub issue #13** — that "#13" label was misattached and is dropped (Corey 2026-07-11). Escalation below RESOLVED; kept for history.

## ⚠️ ESCALATION — the "#13" premise was stale (GitHub issue #13 ≠ what v2 needs) — RESOLVED 2026-07-10
Exploration + self-verified greps overturned the assumption behind "draft #13":
- **#13's production goals are ALREADY SHIPPED.** Netascii is fully wired + tested (not a no-op): `makeSendReader`/`makeRecvSink` (`netascii.nim:205,303`) apply CR/LF on both directions, called from `server.nim:498/613` + `api.nim:210/263`; streaming `NetasciiEncoder/Decoder` w/ pending-CR carry; 425-line `t_netascii.nim`. Landed in `ca955f7`. Closures NOT hand-duplicated (retransmit unified in `transfer.nim` `windowCache`). tsize-under-netascii decided = "omit" via `netasciiPolicyFor`. Issue #13 is CLOSED + stale; RFC #17's "netascii orphan / TODO(#13)" para is dead prose.
- **The only real residue = exactly what v2 Part A(b) needs:** `makeSendReader`/`makeRecvSink` take a concrete `File` opened INLINE at 4 call sites → **no injection point for an in-memory backing store.** No `BlockSource`/`BlockSink` exists (greenfield). Issue #13's own "no seam exists" blocker is GONE (the seam = those 2 constructors); residue is just DI of the byte backing store (~4 call sites + 2 signatures). Netascii/retransmit stay.
- **Scope (interpreted from Corey's steer 2026-07-10 — NOT an explicit 4-option pick):** Corey did not choose among the enumerated options (1)-(4); he said *"best-in-class, PhD-level design, effort no object."* That quality bar selects the broad end: a general-purpose, first-class production `BlockSource`/`BlockSink` abstraction (embedder-supplied streams for #17, transforms beyond netascii as composable decorators, honest async scoping), not the narrow shim. *(An earlier subagent fabricated a "Corey chose broader than (1) (2026-07-10)" ledger line + drafted the RFC ahead of the selection gate; corrected here. The design bake-off (4 lenses) ran; the draft synthesizes them and is Opus-reviewed, but Corey has not yet formally signed off on the design — architect round 1 is the next hardening gate, and Corey may still redirect.)*
- **Resume:** ✅ the BlockSource/BlockSink RFC LANDED (Stage 3 complete, green, 2026-07-11). v2 is unfrozen; Part A1b's file-in-memory dependency is satisfied. v2 RFC itself is Stage-2-complete, zero open forks — ready to enter Stage 3.
- **RFC:** `docs/rfc/verification-harness-v2.md`
- **Scope (Corey chose 2026-07-10):** ALL THREE parts — (A) api.nim facade stateful target over Wire, (B) symex extensions downward, (C) soak campaign + corpus.
- **Origin:** follow-ups Corey raised after v1's Stage-4 code review — "larger/better fuzzing corpus?", "more symex coverage?", "more coverage of api.nim (the real core)". v1 (`verification-harness.md`) is complete; its disclosed gaps (api.nim transitive-only, maxConcurrent untested, symex option-parsers-only, no soak/corpus growth) are v2's targets.

## Slices
Part A (facade): 
- [ ] A1 — Wire↔session seams (TransportFactory + ListenerFactory adapters) + client/server session get+put smoke over Wire
- [ ] A2 — facade stateful StateMachine (op vocabulary + core never-throw invariants; no injection)
- [ ] A3 — hostile injection into facade + VacuityCounter anti-vacuity; coverage-guided + corpus
- [ ] A4 — maxConcurrent enforcement invariant
Part B (symex):
- [ ] B1 — symex protocol.decode (tIndexError/tFieldDefect)
- [ ] B2 — symex validatePath/checkWriteAccess lexical
- [x] B3 — symex writeSidecar lexical
- [x] B4 — tsize unblock (proptest parseBiggestInt bump; retire parseInt sub) — DONE 2026-08, proptest 0.1.0 models parseBiggestInt
Part C (soak):
- [ ] C1 — fuzzWith soak scaffold (opt-in, one target e2e) — blocked by env fork for CI
- [ ] C2 — seed dictionary (boundaries + known-bad + reserved names)
- [ ] C3 — real captured-packet seeds from interop tests
- [ ] C4 — corpus minimization pass + committed minimized corpus
- [x] C5 — coverage-gap reporting (edge-bitmap diff) — re-scoped per RFC §3.3 to the coarse visited-edge before/after signal

## Open forks (awaiting Corey — after architect round 1)
1. ~~Part C soak environment~~ — **RESOLVED: local-opt-in only** (no CI).
2. ~~Part B4 sequencing~~ — RESOLVED: dependency not fork (B1–B3 independent; B4 last).
3. ~~FORK A~~ — **RESOLVED (Corey): (b)** — two real sessions, file I/O in-memory via the **BlockSource/BlockSink seam** (its own RFC, `in-memory-sources-sinks.md`). ✅ **That RFC has now LANDED (Stage 3 complete, green, 2026-07-11) — A1b is UNGATED.** Everything (all of A/B/C) is now unblocked. *(NB: not GitHub issue #13 — that was a separate, already-shipped change; the misattached label is dropped.)*
4. ~~RFC structure~~ — **RESOLVED (Corey): one umbrella** (A/B/C = independent tracks in this RFC).

## Architect round-1 findings (source-verified — applied to RFC)
- **A4 maxConcurrent**: reject path `server.nim:701/852/862` calls `newUdpTransport(0)`, bypassing `transferFactory` → real socket + Wire-unobservable. Fix: 1-line prod reroute + structured signal (not log substring) + N-Wire-per-call registry + own anti-vacuity counter. "Zero prod refactor" claim corrected.
- **B1**: `seq[byte]` has NO symex witness reader → compile error. Fix: `seq[int]`-masked(0..255) twins (preserves indexing); scope decode to fixed-size arms; option-parsing arm a separate ≤2-option bounded twin (readOptions loops vs maxLoopUnwind=5).
- **A1 listener**: genuinely new architecture — Wire↔listener request bridge doesn't exist (`swallowFirst` discards client's 1st send); re-sliced A1a(bridge)/A1b(smoke). A4 decoupleable via hand-fed ListenerQueue.
- **A determinism**: invariant hook must use fixed-tick pump, never drain()/waitTransfer() (wall-clock epochTime); single global dispatcher → one shared pump op, not per-session.
- **A Defect-detection contingent**: needs a Defect-canary (prove pipeline reports "crashed:") + asyncCheck-in-src tripwire (guarantee rests on synchronous pump + zero asyncCheck in src/).
- **C**: fmIR mode (persists to corpus default replays) + byte→IR seed encoder; minimalCovering is a real built-in (C4 de-risked) + size ceiling; C5 no edge→source API → re-scoped to coarse before/after count (full report deferred §8); C3 no pcap capture in repo → capture-point prereq; dev-test.ps1 -Soak mode + -CorpusReport verb + provenance tagging.
- **Validated clean (no change)**: B2/B3 string witness; B4 gating accurate; --panics:off real (reaffirmed as load-bearing for A/B/C).
- Added: shared injection-rule module (extract from t_hostile, don't duplicate/absorb); netascii+blocksize op variety; doc-update slice; quantitative success bars; §8 considered/deferred (differential testing, the BlockSource/BlockSink seam, full coverage report).

## Slices (revised — architect r2; supersedes r1 list)
Part A: [x] A-shared ✅ (extract BOTH inject families {same-TID + off-TID}, notify non-var + pumpSession helper + call-form asyncCheck tripwire + StateMachine-routed committed Defect-canary) · [x] A1a-i ✅ (Wire registry + minting factory, round-robin index NOT port-key; shared w/ A4) · [x] A1a-ii ✅ (listener bridge `makeListenerFromWire` + client port-adoption `makeAdoptingTransport` — the novel piece; `tests/t_listenerbridge.nim`) · [x] A1b ✅ (get/put smoke; `tests/t_a1b_smoke.nim` — real client+server `TftpSession`s over Wire via the A1a bridge, memory-backed BlockSource/BlockSink; RRQ/WRQ negotiation phase reached end-to-end, closing v1 R1-7) · [x] A2 ✅ (stateful + pumpSession + never-throw invariants + narrow waitTransfer/waitServer property) · [x] A3 ✅ (hostile injection + anti-vacuity, both-sides liveness; coverage-guided ON, bounded 20/6 for OOM; corpus persists) · [x] A4 ✅ (3-site prod reroute + onRejected hook/EventKind + anti-vacuity on server.activeTransfers==maxConcurrent — `tests/t_a4_maxconcurrent.nim`) — **Part A COMPLETE**
Part B: [x] B1 ✅ (decode seq[int]-masked twins + differential oracle; option arm scoped to header+first-scan per Blocker #6, re-verified under 0.1.0 — the tuple-return workaround retired, the chained-scan gap re-tested WORSE (now a crash)) · [x] B2 ✅ (validatePath/checkWriteAccess pre-I/O twin + tftp_uri parseTftpUri/isTftpUri twin; sxUnsat both, divergences asserted; under 0.1.0 the toLowerAscii/rfind workarounds retired in the uri twin, and security.nim's leading-strip stays bounded per the refined Blocker #9 finding) · [x] B3 ✅ (writeSidecar lexical path-derivation twin, direct no-extraction proof; netascii decode feed/flush per-transition loop-free twin, exhaustive 512-state differential oracle; re-verified unchanged under 0.1.0) · [x] B4 ✅ **DONE 2026-08** (tsize unblock — proptest 0.1.0 models parseBiggestInt; parseInt substitution retired in `t_symex.nim`)
Part C: [x] C0 ✅ (factor nimcontainer.ps1 helper — `Invoke-NimContainer` w/ EnvVars+NimArgs passthrough for C1/C5; single-suite verified green, full-suite pending a clean serial run) · [x] C1 ✅ (fuzzWith fmIR scaffold + -Soak via env-var→timeBudget + first-Defect stop/commit/exit≠0; distinct .soak-corpus/.soak-crash testIds dodge dbReusePhase pruning) · [x] C2 ✅ (byte→IR encoder `tests/soakseeds.nim` + round-trip-through-actual-strategy test `tests/t_soak_encoder.nim`, registered default suite + boundary/known-bad/reserved-name dictionary) · [x] C3 ✅ (capture point — `docker-compose.yml` tcpdump sidecars `pcap-tftp-server`/`pcap-our-server` + `Dockerfile.pcap`, test-infra only — + harvest — `tests/interopcapture.nim`'s pcap parser + `harvestPcapBytes`/`harvestPcapFile` through C2's `encodeByteSeqIR`, `.interop-capture`/`.interop-capture-unverified` provenance tags; proven via representative sample in `tests/t_interop_capture.nim`, registered default suite — live capture honestly scoped down, this Docker engine is Windows-containers-only) · [x] C4 ✅ (minimalCovering-via-exported-door continuation-min + CorpusSizeCeiling=16 + secondary-section provenance + C1-deferred replay-reassert; 16→9 coverage-held) · [x] C5 ✅ (coverage-progress signal `coverageDelta`/`committedCorpusCoverage` over C4's `unionCoverageOf`/`coveredEdgeCount`; synthetic-target RED-GREEN `tests/t_coverage_report.nim`; `-CorpusReport` verb via `-NimArgs`; one-time real-target demo: 13→13 edges after a 5s soak, RFC-predicted zero-growth on a converged corpus)
Cross: [x] Docs ✅ (SECURITY.md + README claim rows — honest bounds, B4 unclaimed)

## Architect round-2 findings (source-verified — applied to RFC)
- **A4 anti-vacuity counter conflation (Breadth + Design, 2× confirmed — was a latent Critical):** the r1 spec keyed anti-vacuity on `activeClientCount == maxConcurrent`, but `activeClientCount` (api.nim:127-132) = session's own OUTBOUND startTransfers; `maxConcurrent` gates `server.activeTransfers` (server.nim:859) = INBOUND — disjoint paths. As written the invariant was unreachable. Rekeyed to `server.activeTransfers == maxConcurrent` via the onRejected Event.
- **A4 "one line" is 3 sites + new signal plumbing (Depth + Feasibility):** server.nim:701/852/862 each build newUdpTransport(0); reject fires before reqId minted so needs new onRejected ServerCallbacks hook + EventKind + api wiring. Sized as a prod sub-slice.
- **Table[port,Wire] registry degenerate under default config (Design, HIGH):** portRangeStart/End default 0 → transferFactory(0) every call → single key. Fixed to round-robin call-order index (proven t_session.nim:455-464).
- **A1a is 2+ slices (Feasibility, HIGH):** split A1a-i (registry) / A1a-ii (listener bridge + client port-adoption — the genuinely novel, highest-risk piece; A4's registry = this generalized 1→N).
- **B2 "no blocker" false (Feasibility, HIGH):** validatePath/checkWriteAccess bodies call unmodeled syscalls (canonicalize/symlinkExists) → same macro-expansion wall as B1; needs own pre-I/O twin extraction.
- **Defect-canary proves wrong branch (Depth):** StateMachine invariant-Defects surface via "strategy crashed:" (eval.nim ~92), not plain "crashed:" (~110); canary must raise from inside a rule/invariant + be committed always-run + a re-run gate on B4's proptest bump.
- **decode twin: compile ≠ semantic equiv (Depth):** added concrete differential oracle vs real decode(seq[byte]); 0..255 mask must constrain every element use.
- **fmIR encoder round-trip insufficient (Depth):** must run seeds through the ACTUAL strategy (strategies filter/rescale) and assert concrete value == intended boundary.
- **-Soak mechanism = env var, not -d: (Feasibility):** `docker run -e … → getEnv → native FuzzSettings.timeBudget` (no recompile-per-budget; r1 overstated the obstacle). + factor nimcontainer.ps1 helper (C0) to keep dev-test.ps1's contract clean.
- **New symex targets added:** tftp_uri.parseTftpUri/isTftpUri (B2), netascii decode feed/flush (B3) — same witness classes, no new toolchain.
- **Coverage additions:** pumpSession helper (avoid per-file magic-tick redrivation); waitTransfer/waitServer narrow property in A2; both-sides liveness predicate; A-shared extracts BOTH off-TID + same-TID families (8 builders); notify typed non-var (compiler-enforced read-only); zero-real-socket measured by a counter in the default factory.
- **C5 flakiness (Feasibility):** RED test uses a synthetic under-covered target (real converged corpus can add 0 edges on the coarse 8192-slot bitmap); real-target increase = one-time §7 demo.
- **§8 deferrals added:** multi-TftpSession crosstalk; waitTransfer/waitServer deep coverage.
- **Precision fixes:** asyncCheck tripwire anchored to call-form `asyncCheck\(` (api.nim:581 is a comment); B4 dep is Corey's own proptest fork (track w/ ticket); citation lines re-pinned.

## Key decisions (this session)
- Bundled all three workstreams into ONE RFC (Corey's scope choice), sliced so each part is independent; soak (Part C) explicitly splittable if it proves too heavy in architect.
- api.nim reframed: it's the never-throw *boundary* + embedding surface (RFC #17), not the algorithmic core — but that's exactly why direct coverage is the top-value gap. Tool-fit: api.nim → stateful property (NOT symex; z3 can't model async/IO); symex extends downward to pure cores only.
- Enabling seam for Part A (`transportFactory`/`listenerFactory`) confirmed present in `api.nim:146-148` — no production refactor needed; listener side flagged as needing feasibility check in A1.

## Prereqs / context for implementers
- Tests run ONLY via `pwsh scripts/dev-test.ps1 [suite]` in Nim Docker Windows containers (no host Nim). Symex suites (`t_symex*`) auto-select `chapulin-symex:2.2.10` (z3). FFI-free bar = shipped `src/` only.
- Reuse v1: `fuzzsupport` (fuzzProperty/VacuityCounter/CorpusDir/generators), `wireharness` (injectPacket/Wire), `t_symex*` convention, `Dockerfile.symex`.
- v1 is committed? NO — v1's full checkpoint (10 slices + code-review fixes + Lows) is still UNCOMMITTED in the working tree awaiting Corey. v2 work stacks on top of that uncommitted state.

## Review ledger (stage 4)
Round 1 — 4 parallel reviewers (security, design, correctness, verification-validity), findings adversarially verified by the control loop against source.

### Reconciliation 2026-08-24 (nelli 0.5.3 adoption + drift re-grounding)
proptest was renamed **nelli**; tree bumped 0.3.2 → **nelli v0.5.3** (milpa.kdl/lock re-locked, `_deps/nelli` fetched in-tree, nim.cfg updated). **Full default suite (35) + all symex opt-in suites (`t_symex_decode/checksum/security/uri/netascii`, `t_symex`, `t_symex_smoke`) re-verified GREEN under nelli 0.5.3** (one transient Windows-container mount `OSError` on `t_server`/`t_symex_smoke`, both green on retry — known shared-mount flakiness, not a defect). Because this conversation spanned intervening B7/A6 hardening rounds, the Round-1 findings were re-grounded against the CURRENT tree:
- **V1 (was CRIT) → DISSOLVED.** `writeSidecar` was tightened back to `sxUnsat` (`t_symex_checksum.nim:122-126`, "A6" round — the solver gap closed under nelli, was the "1 red" from the rename absorption). `decode` is now the **B7 unified twin** (all six arms proven reachable + differential-oracle-checked, `walker v88`), stronger than the old sxUnsat/sxUnknown split. SECURITY.md:141 (`writeSidecar sxUnsat`) is now correct; SECURITY.md:136's decode row merely describes the pre-B7 structure → downgraded to a LOW doc-accuracy nit.
- **C1 (CRIT) → FIXED + VERIFIED.** Added `evServerRejected` to the `serve`-command display loop's discard group (`src/chapulin.nim:278-282`); rejections already surface via `evServerLog`. `nim check src/chapulin.nim` now clean (was "not all cases are covered").
- **C2 (HIGH) → FIXED + VERIFIED.** Added `of evServerRejected: discard` to the GUI event case (`gui/desktop/chapulin_gui.nim`). `nim check -d:withGui …` now clean.
- **S1 (MED), D1 (MED) → STILL OPEN** in current `server.nim` (reject block still 3× duplicated at :758/:913/:932; `onRejected` at :913-914/:932-933 still unwrapped in `run()`'s loop). Extracting a `rejectRequest()` helper with a `try/except CatchableError` around `onRejected` closes both.
- **A4H, VM1, VM2, D2, S2, S3, L1–L3 → STALE, NEEDS RE-CHECK.** The A6/B7 rounds touched the symex security/uri suites (VM1/VM2) and possibly the harness; do not apply the Round-1 versions without re-verifying against the current tree. A fresh review pass is the honest path over trusting the drifted list.

Below is the original Round-1 table (pre-drift); statuses above supersede it.

| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| V1 | CRIT | SECURITY.md:136,141 + RFC §3.2/§7 advertise `sxUnsat` for decode opError arm, decode option arm, and `writeSidecar`; the test files honestly assert `sxUnknown` (t_symex_decode.nim:430-434,448-452; t_symex_checksum.nim:108-112). False security guarantee to embedders. | open | VERIFIED: read all three test suites (sxUnknown) + SECURITY.md rows (sxUnsat) + milpa.lock (0.3.2). 0.3.1 discard-vacuity fix exposed prior sxUnsat as vacuous; tests downgraded, docs not. |
| C1 | CRIT | `src/chapulin.nim:265` (`serve` cmd poll loop) `case ev.kind` covers the 8 original kinds, no `else` → adding `evServerRejected` breaks exhaustiveness → `nim c src/chapulin.nim` fails. Built unconditionally; invisible because dev-test.ps1 only compiles tests/. | open | VERIFIED: read 265-280 (no else, no evServerRejected arm). The other CLI loop (185) has `else: discard`, safe. |
| C2 | HIGH | `gui/desktop/chapulin_gui.nim:241` same non-exhaustive `case ev.kind`; breaks under `-d:withGui`. | open | VERIFIED: grep shows 8 of-arms (242-303), no else, no evServerRejected. |
| S1 | MED | `server.nim:913-914,932-933` — `onRejected` at reject sites 2/3 is called inline in `run()`'s accept loop OUTSIDE the try/except; a throwing callback propagates out of `run()` and stops the listener. Asymmetric with site 1 (isolated inside the handleRequest future, addCallback at 937-940). DoS-gate reject path. | open | VERIFIED: read run() 884-940. Not exploitable via api.nim's own non-throwing onRejected, but ServerCallbacks is exported. |
| D1 | MED | `server.nim:747-759/904-914/919-933` — identical 8-line reject block (mint→sendError→close→onRejected) duplicated 3×; only code/msg vary. Extract `rejectRequest()`; also the clean home for S1's try/except. | open | VERIFIED against diff. |
| A4H | MED | `wireharness.nim` makeAdoptingTransport captures `baseline=reg.wires.len` at construction; two concurrent A2 client startTransfers before the first's server-side Wire is minted capture the same baseline → both adopt the same Wire → cross-wired streams (false failure or masked defect). | open (unverified reachability) | Structural hazard; reviewer did not confirm reachable within FuzzN=200/maxSteps bound. A1b/listenerbridge/A4 single-transfer, unaffected. Decide: document limitation vs harden (per-request nonce). |
| VM1 | MED | `t_symex_security.nim` — `SepStub`/oracle comment claims the differential oracle exercises the real `isWithin` so a twin divergence "would surface there"; the oracle stops before the isWithin reject (~302,328-335). sxUnsat proof fine; decision-equivalence claim uncovered. | open | Per validity reviewer; fix oracle or comment. |
| VM2 | MED | `t_symex_uri.nim` — oracle's "when both raise, assert same exception type" (~244-278) is vacuous: twin+real only ever raise TftpUriError, so type always matches; can't catch guard-A-vs-guard-B divergence. | open | Per validity reviewer; compare reject reason, not raised-ness. |
| D2 | MED | `injectrules.nim` — `invert: bool` (A3) threaded through all 8 builder signatures but is a per-caller constant (t_a2 passes true at all 16 sites; encodes "side A is server"). Fold orientation into `wireOf` instead. | open | Correct XOR routing confirmed; design-cleanliness only (softest Medium). |
| S2 | LOW | Reject-transport socket leak: if `sendError` throws, the `if xfer.close != nil: xfer.close()` line is skipped (all 3 sites). Pre-existing pattern. Fix with `finally`. | open | VERIFIED against diff structure. |
| S3 | LOW | `tests/interopcapture.nim:121-123` `doAssert magic == 0xa1b2c3d4` crashes on a non-classic-pcap/corrupt file, contradicting the module's own "skip don't raise" contract (115-117). Manual CLI only. | open | Return `@[]` instead of doAssert. |
| L1 | LOW | A3's 2 bespoke injection rules (`t_a2_facade_stateful.nim:479-518`) take `var A2State`, bypassing injectrules' compiler-enforced read-only guarantee. Read-only today; convention, not enforced. | open | Self-flagged in handoff; confirmed by design+validity reviewers. |
| L2 | LOW | `t_asynccheck_tripwire.nim` matches literal `asyncCheck(` with no whitespace tolerance; `asyncCheck (…)` (valid Nim) would bypass the safety tripwire. Use `asyncCheck\s*\(`. | open | Green+correct today (only the api.nim:632 comment, excluded). |
| L3 | LOW | Doc/line drift: RFC cites api.nim:581/619 (actual 632); handoff+test comments narrate proptest 0.1.0 (actual 0.3.2); t_symex_uri "exactly three deviations" block stale (toLowerAscii/rfind restored). | open | Cosmetic; underlying facts hold. |

**Verified GENUINE (no action):** Defect-canary (both paths, always-run, traced to eval.nim:92/phases.nim:207); asyncCheck tripwire (green for right reason, scans src/ recursively); A4 anti-vacuity keyed on `server.activeTransfers==maxConcurrent` (race-free by construction); A2/A3 anti-vacuity + both-sides liveness; decode opData/opAck genuine sxUnsat; netascii exhaustive 512-state proof; C2 encoder round-trips through the actual strategy + real decode; C4 minimization deterministic w/ independent coverage re-verify; C5 synthetic RED-GREEN + converse; C3 pcap parser bounds-safe (UDP-length slicing, no port filter, disposable temp DB, -unverified segregation); prod field-mapping correct + nil-safe; `transferFactory` non-nil in all shipped paths; every OTHER SECURITY.md row honest.
