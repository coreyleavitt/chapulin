# Verification harness — handoff

- **Stage:** 4 (`/code-review`) — **COMPLETE. Loop reached the floor (round 3: 0 Crit/High/Med).** All
  Stage-3 slices + all Stage-4 fix batches GREEN. Nothing committed (awaiting Corey). RFC implementation +
  review both done; remaining work is the two standing items (commit checkpoint; proptest-manifest PR).
- **Stage 3 was:** COMPLETE, all 10 slices landed and green (see slice checklist below).
- **D8 decision (2026-07-10): Corey accepted BOTH halves in scope, nothing deferred** — payload injection = slice 7, off-TID (+ `Wire`/`Transport` source-address extension) = slice 8. Symex = slice 9, docs = slice 10.
- **Resume:** Stage 4 — `/code-review docs/rfc/verification-harness.md` (user-triggered; billed). Two standing items for Corey: commit this checkpoint (now all 10 slices); open the proptest-manifest PR to retire the 3-pin shim.
- **Coverage gaps slice 10 surfaced (honest, for code-review to weigh):** (1) `api.nim` facade boundary is only covered transitively — nothing fuzzes `request`/`poll`/`drain` directly; (2) `maxConcurrent` *enforcement* is untested — only config-plumbing is verified, not that the (N+1)th transfer is actually rejected (RFC scoped this out as a non-goal, but the gap is now visible not papered over). Symex follow-up: `parseBiggestInt` unmodeled in proptest 99fa2dbe → tsize twins use `parseInt` (sound sub); revisit on a proptest bump.
- **Round 3 (applied, design/ergonomics lens):** RFC restructuring — trimmed "Verified before drafting"'s z3 saga (was ~80 lines of debugging-diary prose inline) down to a summary bullet, moved the full narrative to new **Appendix A** at the doc's end (facts vs. history now separated). D2 — replaced the "one `.nim.cfg` per coverage-guided file" design with a single directory-level `tests/nim.cfg` (Nim auto-loads a bare `nim.cfg` for every file in the module's directory, not just `<module>.nim.cfg`) + a shared `tests/fuzzsupport.nim` exporting a `fuzzProperty(name, testId, body)` template that bundles the `Settings(coverageGuided, testId, dbPath, seed, maxExamples)` clause and the `currentCoverage()>0` tripwire — removes 4+ near-identical `.nim.cfg` files and 4+ hand-copied tripwire asserts down to one shared template + one shared cfg. `dev-test.ps1` — replaced the `$suiteImage` exact-name hashtable with a `$t -like "t_symex*"` prefix convention (code change applied directly, not just documented) so a future `t_symex_foo.nim` auto-selects the z3 image instead of silently falling back to the base image on a forgotten map entry. `.gitattributes` — `tests/corpus/** -text` changed to `tests/corpus/** binary` (git's built-in `-diff -merge -text` macro): bare `-text` stops CRLF normalization but leaves diff/merge to git's own binary-sniffing heuristic, which a short corpus entry (version byte + a few choice bytes) isn't guaranteed to trip, so a merge conflict on a corpus file could silently splice byte streams instead of hard-conflicting. `Dockerfile.symex` / `t_symex_smoke.nim`(+`.cfg`) reviewed — already clean, no changes.
- **Round 2 (applied):** symex fork RESOLVED (proven working, see below). Clear-best batch applied — D3 fixed-fixture corrected (fuzz lexical; symlink refusals stay in dynamic e2e tests), per-domain `.nim.cfg` + `currentCoverage()>0` tripwire, target→file mapping, D6 drop-accounting scoped to after-push, `.gitattributes tests/corpus/** -text` (created), `dev-test.ps1` suite→image map, CI-prereqs recorded, D2 MSVC-downgrade + determinism-closed, slice 8 split, low adds (dropped shipped checkHostAccess, added redactRoot). (Superseded by round 3's `.nim.cfg`/tripwire consolidation and `.gitattributes`/`dev-test.ps1` fixes above.)

## z3/symex toolchain status (2026-07-10) — Windows/MSVC WORKING END-TO-END
- Local milpa shim: chapulin `milpa.kdl` dev-deps has `z3 git=…/nim-z3 ref=6a708ed` + `softlink v0.5.1` root override (proptest's main still pins z3 by dead `local=`; nim-z3 pins softlink v0.5.0, proptest pins main — override forces v0.5.1). **TEMP** — remove once proptest's manifest flips z3 to git= and aligns softlink.
- Built `Dockerfile.symex` (nim base + z3 **4.13.4**, image tag `chapulin-symex:2.2.10`, Windows engine `desktop-windows`). Smoke probe `z3smoke.nim` (+`.cfg` = just `--cincludes:"C:/z3/include"`) compiles under `nim c` with REAL softlink verification and prints `Z3 4.13.4.0`.
- **RESOLVED:** the MSVC compile-time `_Static_assert` blocker is fixed upstream by **softlink v0.5.1** + **nim-z3 6a708ed** (const-tolerant MSVC verify; no `/std:clatest` needed). No stub, no ABI wall.
- **Symex ENGINE fragment: RESOLVED + PROVEN (2026-07-10).** The earlier "compile-errors on parseInt/toLowerAscii, needs `{.symexOpaque.}` wrappers, yields sxUnknown" verdict was a STALE-PIN artifact (read frozen `_deps/proptest` 303185, not current main). Bumped chapulin's proptest pin to **99fa2dbe** (models parseInt via Z3 str.to.int + raise-fork, toLowerAscii, try/except, zero-init vars). Existing property suite (t_props/transfer/server/netascii/session) still green under the bump. A throwaway probe over chapulin's blocksize `parseInt`+bounds+raise idiom returned `sxSat` (witness "0") and `sxRaised` (witness "") in the z3 image, each validated against real Nim. Soundness review also surfaced+fixed a real proptest bug (`discard parseInt(s)` dropped the raise). So slice 7 = real proof/witness targets, NO wrappers, NO gating. Fork #1 (symex) RESOLVED.
- **(3) chapulin-side DONE (2026-07-10):** probe folded into `tests/t_symex_smoke.nim` (+`.cfg`), passes via `dev-test.ps1 -Only t_symex_smoke -Image chapulin-symex:2.2.10` (new `-Image` param; kept out of the default array so the base suite is untouched). Verified green through the real runner.
- **Shim removal (pending Corey's repos):** dropping chapulin's `milpa.kdl` z3/softlink shim needs proptest's manifest to flip `z3 local=` → `git=…/nim-z3` and align its softlink pin so no root override is needed. Cross-repo coordination — offer a proptest PR; keep the TEMP shim meanwhile.
- Uncommitted: `Dockerfile.symex`, `tests/t_symex_smoke.nim`(+`.cfg`), `scripts/dev-test.ps1` (-Image), `milpa.kdl`/`milpa.lock` (shim), plus the RFC + handoff. Not yet committed (awaiting Corey). See [[softlink-dynlib-lib]].
- **RFC:** `docs/rfc/verification-harness.md`
- **Origin:** the proptest/interop/fuzzing gap survey (2026-07-06). chapulin uses only proptest's `forAll` surface; the coverage-guided path + z3 symex are unused, and the never-throw-Defect security property (the basis of `SECURITY.md`) is under-verified.

## Round 1 outcome (2026-07-06)
Four-lens team (Depth/Breadth/Design/Feasibility) reviewed against source. Clear-best fixes **applied to the RFC**; two escalations surfaced.

**Applied (clear-best):**
- **Re-scoped D1/D5 around `Settings.coverageGuided` + `testId`/`dbPath`** — proptest's *existing* `forAll`/`property` surface already gives coverage-guided search AND `directoryBasedDatabase` corpus persistence for free (the source audit had missed `coverageGuided`). Default path is now "add a `with Settings(...)` clause to the `t_props.nim` properties we already have," not a bespoke `t_fuzz.nim`. `fuzzWith` demoted to opt-in soak.
- **Per-target oracles (D3 table)** — `decode`→`TftpDecodeError` only; `negotiateServerOptions`→`ValueError` only (raises bare by design); `validateAndParseOack`→total; containment→never-escape + reserved-`.md5` forgery-refusal; new targets netascii decoder + `checksum.writeSidecar`.
- **Deterministic green condition** — fixed seed + finite examples + assert `crashes==0` **and** `iterations>0` (guards vacuous zero-iteration green); zero-init `FuzzSettings` hang called out for soak mode.
- **Per-file `-d:chapulinFuzz` via `tests/t_props.nim.cfg`** (dev-test.ps1 can't express a per-suite flag); D2 shim moved to one shared exported `coverpragma` module; accepted src/ annotation cost stated honestly.
- **D6** — safety form `droppedLogCount>0 ⇒ order.len==cap` (not untestable "eventually surfaces"); must carry over the coalescing invariant; `forAll` over op-seq, not `stateful`.
- **Fixed fs fixture tree** for containment targets + OS-tagged corpus entries.

## Slices (current — authoritative detail in the RFC; round-3 revised)
- [x] 1 — Scaffold: `tests/nim.cfg` (-d:chapulinFuzz) + `src/chapulin/coverpragma.nim` + `tests/fuzzsupport.nim` (`fuzzProperty` — MACRO not template, bindSyms deps) + `{.cover.}` on `protocol.decode`. GREEN via `dev-test.ps1 -Only t_props`; prod build FFI/proptest-free. Design calls: fuzzProperty macro (template's 2-elem body rejected by `property`); no-op pragma via `template cover*() {.pragma.}`; fixed latent `.gitignore` `nim.cfg`→`/nim.cfg` (was silently dropping `tests/nim.cfg`).
- [x] 2 — Option oracles (`negotiateServerOptions` ValueError-only, `validateAndParseOack` total) + `{.cover.}` on both in `src/chapulin/options.nim`; new `fuzzedOptionPairs` generator reaches the parseInt raise arms. GREEN via `dev-test.ps1 -Only t_props` (16 props). NOTE for slice 3: full-suite `t_security` Cleanup fails `Access is denied` on a leftover symlink temp dir — PRE-EXISTING (commit 749555e, Windows junction-removal), not from slices 1–2; slice 3 should handle it since containment lives in t_security.nim.
- [x] 3 — Containment LEXICAL: `security.validatePath.lexical` + `security.checkWriteAccess.reservedLexical` fuzz targets in `t_security.nim` vs fixed `tests/corpus/fixtures/root/` tree; `{.cover.}` on both fns; symlink e2e preserved. Fixed PRE-EXISTING junction-cleanup bug (`removeSymlinkSafely` → `cmd /c rmdir` for dir reparse points). Retired 2 superseded `validatePath` props from t_props (no-dup-oracle). FULL 17-suite run GREEN.
- [x] 4 — Netascii decoder/encoder fuzz (`t_netascii.nim`) + `checksum.writeSidecar` lexical vs disposable temp root (`t_checksum.nim`) + plain `redactRoot` (`t_server.nim`) + new `t_format.nim` `sanitizeForDisplay` props; `{.cover.}` on netascii feed/flush + writeSidecar; `t_format` wired into dev-test.ps1. 18-suite run GREEN. All 4 fn names matched source. Runner note: use `& script -Only @('a','b')` (array, not comma-string) or extra values misbind to -Image.
- [x] 5 — `.gitattributes tests/corpus/** binary` confirmed already in the working tree (untracked; commit-order note below). New `tests/t_corpus.nim`: (a) synthetic `directoryBasedDatabase` save→reload proof via `proptest/choice` hand-crafted `ChoiceNode`s (embedded NUL/LF/CR/0xFF bytes prove the write/read path is binary-safe; re-save reproduces byte-identical file contents — deterministic encoding); (b) all 8 committed `tests/corpus/*.bin` entries (slices 1-4) reload via two independent `directoryBasedDatabase` handles with identical `loadPrimary`/`loadSecondary` results both times (real-corpus determinism proof, not just the synthetic stand-in). `fuzzsupport.nim`: added `OsTag`/`osTaggedId` — the OS-tagging *mechanism* the RFC asks for — but deliberately NOT applied to any of the 8 existing targets: each target's oracle already asserts an OS-independent invariant (documented precedent: `t_security.nim`'s `validatePathOracle` doc comment already argued this for the Windows-only `:`/ADS branch), so no witness misreplays cross-OS today. Wired into `dev-test.ps1`'s array. 19-suite run GREEN. **Commit-order note for Corey:** stage `.gitattributes` in the same commit as (or an earlier commit than) `tests/corpus/**`, or the binary attribute won't apply retroactively to history already written without it.
- [x] 6 — Event-queue property in `t_eventqueue.nim`: op-seq `forAll` (per D6, NOT stateful) — after-every-op H1 safety (cap never exceeded), push-scoped drop-accounting (dropped-count warning), per-id coalescing (position-pinned order), never a Defect. Full suite GREEN (confirmed from transcript; agent idled on its own bg run).
- [x] 7 — Hostile-session payload injection (D8a): `tests/t_hostile.nim` — proptest `stateful` StateMachine drives live `sendBlocks`↔`recvBlocks` over `Wire`, injects forged/garbage DATA/ACK/ERROR (8 rules, 200 examples); never-Defect via dispatcher-pump in `invariant` + proptest's `except Defect`. Added `injectPacket*(w, toSideA, data)` to `tests/wireharness.nim` (standalone Wire affordance, bypasses waPass/Drop/Dup/Delay schedule); `t_hostile` wired into dev-test.ps1. 21 suites / 560 checks GREEN. Slice-8 hook: injectPacket doc notes off-TID needs a source-address dimension (currently fixed "peer"/0).
- [x] 8 — Hostile-session off-TID (D8b): extended transport recv (`makeTransport`/`makeFailingTransport` `doRecv` now unpack `(data, sourceAddr)`) with a per-packet source-address dimension; off-TID DATA/ACK/ERROR/garbage is rejected by the TID lock, never a Defect; off-TID DATA bounced (never delivered, never derails the legit transfer). **TID lock IS enforced — no wrong-spec.** Full 21-suite run GREEN.
- [x] 9 — Symex proof/witness in `tests/t_symex.nim` (+`.cfg`; z3 image via `t_symex*` convention): 8 per-arm `val:string` twins (client `validateAndParseOack` + server `negotiateServerOptions`) × {tRaisedExn/tIndexError/tFieldDefect} = 24 checks GREEN. Client try/except arms → `sxUnsat` (no-ValueError proof); server uncaught arms → `sxRaised` witnesses validated vs real Nim; index/field → `sxUnsat` all 8. NOT added to default $tests (symex opt-in, needs z3 image). FOLLOW-UP: `parseBiggestInt` unmodeled in proptest 99fa2dbe → tsize twins substitute `parseInt` (sound, same ValueError/64-bit); revisit on a proptest bump that models parseBiggestInt.
- [x] 10 — Docs + SECURITY.md claim→target cross-ref: added a `## Verification` section to `SECURITY.md`
  (claim-by-claim table, exact `testId`/property/suite strings verified against the slice 1-9 source) and
  a "Verification harness" section to `README.md` (how to run the fuzz suite via `dev-test.ps1` and the
  opt-in symex suite via the `t_symex*`/`-Image` convention, corpus location, the never-throw-Defect
  property in one sentence). Design call: discovered SECURITY.md never actually named the TID-lock control
  slice 8 verifies (`transfer.nim`'s `recvOnce`, RFC 1350 §5) — added it as a new Authorization bullet
  before cross-referencing it, rather than cross-referencing a claim that didn't exist in the doc. Two
  claims called out with **no verifying target** (not papered over): `api.nim`'s own facade boundary
  (coverage today is transitive through the layers it wraps — `t_hostile.nim`'s `sendBlocks`/`recvBlocks`
  state machine plus the individual parser fuzz targets — but nothing fuzzes `request`/`poll`/`drain`
  directly); and `maxConcurrent`'s *enforcement* (only config-plumbing is tested, not that the (N+1)th
  concurrent transfer is actually rejected/queued — the RFC scoped this out as "not a parse/Defect
  surface," a reasonable call for this RFC, but the gap itself is real). Markdown-only; no test run needed.
  ALL 10 SLICES COMPLETE — RFC done.

## Open forks
**None — all resolved (symex + D8 both in scope). RFC ready for `/tdd`.**

## Review ledger (stage 4)
Round 1: 4 reviewers (Correctness / Security / Design / Verification-rigor) + adversarial verifier on the
t_hostile cluster (A/B/C all HELD). Presented to Corey; awaiting fix mandate.

| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| R1-1 | Critical | `t_hostile.nim` D8a payload-injection property is structurally vacuous: injects only hit the post-transfer dally, never a live in-flight window; `ensure final.w != nil` can't detect it. | **fixed** | Batch 1 (agent ab3362d1): run-level `d8aLiveInjections>0` anti-vacuity check (injections proven to reach a live DATA/ACK window); scenario reworked. Full suite GREEN — "[OK] payload injection reaches a LIVE in-flight transfer..." |
| R1-2 | High | `README.md`:154-159 lists `t_hostile` as coverage-guided/`{.cover.}`/corpus-persisted — was none. | **fixed** | Fixed by making code true to doc: t_hostile now routed through `fuzzProperty` (coverageGuided+testId+dbPath) → README claim now accurate; corpus entries `hostile.payloadInjection.bin` + `hostile.offTidInjection.bin` exist |
| R1-3 | High | D8b off-TID *fuzz property* had no anti-vacuity assertion; only the deterministic companion asserted `aBounced==1`. | **fixed** | Run-level `d8bLiveInjections>0` + `d8bBounceTotal>0` checks added to the property. GREEN — "[OK] off-TID injection reaches a LIVE in-flight transfer and the TID-lock mismatch arm genuinely fires across the run" |
| R1-4 | Med | Hostile targets were the only fuzz targets not coverage-guided or corpus-persisted. | **fixed** | Both properties now `fuzzProperty` w/ `currentCoverage()>0` tripwire + `{.cover.}` on `sendBlocks`/`recvBlocks`/`recvOnce` (transfer.nim, release no-op). 2 hostile corpus entries committed |
| R1-5 | Med | `t_corpus.nim` replay list omitted `eventqueue.opseq` + the 2 new hostile entries; "every committed... replays deterministically" silently under-checked. | **fixed** | Batch 2 (agent a2be0714): hardcoded list replaced with `corpusTestIds()` deriving from `walkFiles(CorpusDir/"*.bin")` + non-empty guard (can't drift). Full suite GREEN — "[OK] every committed fuzz-target corpus entry reloads... replays deterministically" over all 11 entries |
| R1-6 | Med | Generator-strategy boilerplate copy-pasted across t_props/t_hostile/t_security/t_checksum; t_hostile redefined `toByteSeq*` that `wireharness` exports. | **fixed** | Batch 2: 5 helpers hoisted to `fuzzsupport.nim` (exported); `wireharness` re-exports `toByteSeq` (no ambiguity); local copies deleted; genuinely-different alphabets left alone; defaults preserved. Full suite GREEN |

**Round-2 re-review (DONE):** Verification-rigor + Security + Design over changed scope. **Fixes certified GENUINE** — anti-vacuity counters proven diagnostic (Security: keys on `not serverFut.finished`, not gameable; Verif-rigor: old scenario would've yielded 0/failed; `d8bBounceTotal` traces to real TID-mismatch bounce), `src/` release no-op confirmed, no generator narrowing, corpus derivation correct, no new overclaim. NO Critical/High. New round-2 findings below (all Med/Low, mostly batch-2 refactor artifacts).

| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| R2-1 | Med | `wireharness → fuzzsupport` re-export (from R1-6) pulls all of proptest transitively into non-fuzz consumers (`t_wireharness.nim` had zero proptest dep). Backwards layering. | **fixed** | Batch 3 (agent a9d672a8): removed the import/re-export; `t_props_transfer`/`t_props_server` import `fuzzsupport` directly. Round-3 Security+Design confirm `t_wireharness`/`t_session` now proptest-free, zero coverage loss. Full suite GREEN |
| R2-2 | Med | Cross-file dup reintroduced: `t_security` `RawAlphabet`/`rawPathStrings` ≡ `t_checksum` `RawSidecarAlphabet`/`rawSidecarStrings` (byte-identical). | **fixed** | Batch 3: hoisted `RawPathAlphabet`/`rawPathStrings(maxLen=24)` to fuzzsupport; t_checksum call site passes `maxLen=20` to preserve reach; both local copies deleted. Round-3 confirms no narrowing. GREEN |
| R2-4 | Med | Anti-vacuity counters bespoke module-level `var`s + implicit source-order contract. | **fixed** | Batch 3: minimal `VacuityCounter{count}`+`note()`/`assertFired()` in fuzzsupport; `inLiveDataPhase`/note timing UNCHANGED. Round-3 Security certifies hard-fail preserved (`check`, genuine fail on 0). GREEN |
| R2-3 | Low | `corpusTestIds()` filename→testId `safeKey`-char coupling (fails loud, not silent). | **fixed** | Batch 3: clarifying comment added |
| R2-5 | Low | `inLiveDataPhase` few-tick boundary imprecision (statistically immaterial). | **fixed** | Batch 3: clarifying comment added |
| R2-6 | Low | SECURITY.md "refuse `..` traversal" wording slightly more specific than the property pins (source `".." in cleanName` refusal confirmed unchanged). | deferred (Low) | Security re-review; source behavior correct |
| R3-1 | Low | `VacuityCounter` hoisted to shared module with one current consumer (mild YAGNI). | deferred (Low) | Round-3 Design; "not worth reverting" |
| R3-2 | Low | `fuzzsupport.nim` (~200 lines) accumulating orthogonal concerns; split into `fuzzgens.nim` vs settings/bookkeeping IF it grows further. | deferred (Low) | Round-3 Design; forward-looking, no action now |

### Stage 4 outcome — CODE-REVIEW LOOP COMPLETE (floor reached)
**Round 3 re-review surfaced 0 Critical / 0 High / 0 Medium — only forward-looking Lows.** Termination floor met per `/code-review` Step 7. Full suite GREEN across all three fix batches (20 default suites; symex suite opt-in/untouched).
- **Rounds:** R1 (4 reviewers + adversarial verifier) → 1 Crit / 2 High / 4 Med / Lows, all fixed. R2 re-review certified R1 fixes GENUINE, found 3 Med (batch-2 refactor artifacts) + Lows, all fixed. R3 re-review = clean (Lows only).
- **Note:** a stale batch-1 agent overlapped edits with batch-2/3 on shared files; reconciled — static coherence check (no conflict markers / dup defs) + GREEN suite confirm the tree is coherent.
- **Nothing committed** (awaiting Corey's explicit go-ahead). Standing items still open: commit the full checkpoint; open the proptest-manifest PR to retire the 3-pin milpa shim.
- **Lows — dispositioned (Corey: "fix lows now", 2026-07-10):**
  - **FIXED:** R1-10a — added an `osTaggedId` unit test to `t_corpus.nim` (guards the suffix logic while unwired). R1-10b — `dev-test.ps1` now splits a comma-joined `-Only "a,b"` string + documents the array form / space-separated footgun. R2-6 — SECURITY.md `validatePath` row reworded to the no-escape-invariant framing (matches what the property proves).
  - **NOT fixed (deliberate, not polish):** R1-8 (.gitattributes/corpus commit-ordering — nothing to edit; resolves when the checkpoint is committed with `.gitattributes` staged first). R1-9 (Windows reserved device names — closing it is a real `src/` containment *feature* with its own design questions, a separate hardening item, not a test-polish Low; pre-existing & documented in SECURITY.md residuals). R1-11 (parseBiggestInt sub needs a cross-repo proptest bump; `api.nim` facade fuzz target + `maxConcurrent` enforcement test are disclosed follow-ups / RFC non-goals, each a new target not a polish fix). R3-1 (VacuityCounter one-consumer hoist — reviewer explicitly said "not worth reverting"; fixing = undoing a good refactor). R3-2 (split `fuzzsupport.nim` only *if it grows* — premature now).
| R1-7 | Med | `handleRrq`/`handleWrq` named in RFC D8 but never exercised; scope narrowing undisclosed. | **fixed** | SECURITY.md Verification table now explicitly documents the narrowing (hostile harness = post-negotiation data phase; RRQ/WRQ negotiation phase = tracked follow-up needing a real OACK-speaking client + per-example disk setup). Sound scope call, disclosed not silent |
| R1-8 | Low | `.gitattributes` + `tests/corpus/**` still untracked → commit-order hazard live until staged together (CRLF could no-op a binary corpus entry). | open | Security (Low), operational |
| R1-9 | Low | Windows reserved device names (CON/NUL/COM1) not fuzzed — pre-existing, documented residual in SECURITY.md, not a false claim. | wontfix (pre-existing, out of RFC scope) | Security (Low, informational) |
| R1-10 | Low | `osTaggedId` (fuzzsupport) has no unit test; `dev-test.ps1 -Only` comma-vs-array footgun undocumented in-script. | open | Design (Low ×2) |
| R1-11 | Low | `parseBiggestInt`→`parseInt` symex sub; `api.nim` facade + `maxConcurrent` gaps — all confirmed honestly disclosed, correctly scoped. | wontfix (honest, scoped) | Verif-rigor confirmed non-issues |
