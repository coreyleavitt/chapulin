# TFTP RFC-conformance closure — handoff

- **Stage:** 4 code-review **COMPLETE** — fix loop hit the floor (0 Critical/High/Medium) after 4 rounds. Only Lows remain (deferred to Corey).   •   **Round:** review R1→R4 done; High→High→Medium→Low-only convergence.
- **Resume:** Corey's decision — (a) commit the working tree to `main`, and/or (b) optionally mop up the remaining Lows (see "Remaining Lows"). Nothing committed yet.
- **Verified:** full `dev-test.ps1` green across all 15 relevant suites after every fix batch; `t_security` only ever fails its Cleanup junction-removal step (pre-existing flake, 23/23 assertions pass).
- **RFC:** `docs/rfc/rfc-conformance-closure.md`
- **Uncommitted:** all 9 slices are on the working tree, NOT committed (per standing rule — Corey commits to `main` himself when he asks).
- **Known non-issue:** `t_security` fails only in its Cleanup step (Windows-container junction removal `Access is denied`) — pre-existing on unmodified `main`, unrelated to this RFC.

## Slices (re-sliced after round 1: 7→7a/7b, added slice 0)
- [x] 0 — Option-bounds single source of truth: bounds+predicates relocated to `protocol.nim` (incl. `Default*` consts — zero-diff via `transfer.nim` re-export); `server_config.nim` imports `protocol`; `serverConfigBoundsValid` guard in `startServer` + `handleRrq/Wrq`. Tests: t_transfer (pure) + t_session (rejection). Green in Docker.
- [x] 1 — Error code 8: `errOptionNegotiation=8` + decode `<=8` + `clientSafeError` arm + 2 server catches converted 4→8; `TransferInfo.errorCode` added & populated (Q1/A), `api.nim` un-hardcodes 0; `mkTransferInfo` helper extracted. Green in Docker. **NB (stage-4 confirm):** code-8 path now calls `onStart` to mint TransferId *before* failing (else Invariant-4 drops it) → option-neg failures now emit a start+error pair; scoped to code-8 only, all other pre-start drops preserved.
- [x] 2 — Client OACK validation → ERROR(8): pure/total flat `validateAndParseOack` on raw pairs (filter-unrequested, dedupe, case-insensitive, bounds); `applyOack` now takes `NegotiatedOptions` (no `pkt` param) → R4 enforcement is *structural*, not call-site discipline; `sendOackFailure` unifies both old silent ValueError paths. t_options (13) + t_client. Green in Docker.
- [x] 3 — timeout validate **and apply**: server seeds default from `limits.timeout` + drops out-of-range (R6); client clamps in `newTransferConfig`; `neg.timeout` wired into `applyOack`+`handleRrq/Wrq` *before* the post-OACK ACK(0) wait; `effectiveTimeout` = `max(adaptive, config*1000)` floor. Behavioral apply-assertions (recv-timeout recording in MockTransport + wireharness) prove it both sides. Green in Docker.
- [x] 4 — RFC 7440 gap-ACK: `pkt.blockNum > expectedBlock` branch, single-fire mid-window / `dupAckThreshold`× at boundary (keyed on `lastAckedBlock == target`); `dupAckThreshold` hoisted to shared top-level const; `lastInOrderBlock` helper (guarded uint16) fixes the duplicate branch's wrong-block echo too; `lastGapAcked` keyed on current `expectedBlock`. Single-fire reasoning verified against the real `sendBlocks` classifier. Green in Docker (issue-#18 regressions intact). **NB (stage-4):** mid-window single-fire's *instant* resend needs `not hitFinal`; a gap in the transfer's LAST window recovers via RTO/dup-ACK instead — pre-existing `sendBlocks` gating, orthogonal to D6, non-goal boundary.
- [x] 5 — Bounded final-ACK dally: `recvOnce` extracted (flat `RecvOutcome{ok,pkt}`, no auto-resend/raise); `recvPacket` rewritten on it with the 3 named tests green & UNCHANGED; `dallyAfterFinalAck` (deadline = one `effectiveTimeout` AND `MaxDallyReacks=2`, off-target ignored) wired into `recvBlocks` epilogue + single-block GET. **NB (stage-4):** dally's extra `await` exposed a latent `handleWrq` flush race (file flushed only on `recvBlocks` return, after the ACK) — fixed by flushing in `onData` at the final block. Green in Docker.
- [x] 6 — Stateful netascii transform: `NetasciiEncoder`/`Decoder` + `feed`/`flush`, deferred-CR contract (never in-call lookahead), total on all 256 bytes; old zero-caller procs → one-shot wrappers; R2 round-trip proven via proptest (chunk-boundary-agnostic) + documented-collapse asserts; edge (CR+neither) = literal CR, RFC-faithful. `t_netascii` (33) added to `dev-test.ps1`. Green in Docker.
- [x] 7a — netascii send side: `netasciiReader` (two-arg, read-ahead only-0-byte-EOF w/ regression test, ascending doAssert); policy seam = single-source `NetasciiPolicy`/`netasciiPolicyFor(mode)`; RRQ-serve + client PUT-read routed; `.md5` sidecar skipped + `tsize` dropped (server OACK + client outbound); dir-listing one-shot `toNetascii`; PUT `total=none`. Retransmission-under-netascii test green. All 15 suites green in Docker. **Tooling note:** `dev-test.ps1` with multiple suite-name args only runs the FIRST — run suites individually or use the no-filter full run.
- [x] 7b — netascii recv side: decoder-wrapped `onData` write; shared `finishNetasciiDecode(file, dec, success)` (success-only terminal flush) called by both `handleWrq` & `tdGet`; `NetasciiPolicy.decodeWrites` seam; GET tsize-suppression already covered by 7a's shared `clientBuildOptions` (pinned by test). Terminal flush ordered inside `onData` at final-block (consistent w/ slice-5 race fix). WRQ+GET CRLF↔LF & CR-NUL round-trip, zero-length boundary tested. Full-suite green in Docker. **Closes #13.**

## Open forks (awaiting Corey)
- None. **Q1 → Option A** (Corey chose): additive `TransferInfo.errorCode: int`, populated server-side, lands in slice 1.
- Six **resolved decisions** R1–R6 (recommended; veto-able) — see RFC "Resolutions". R4 is *enforced* (filter unrequested),
  R5 dally bound = deadline + `MaxDallyReacks=2`, R6 = server option-failure policy.

## Key decisions (this session)
- Scope = 6 audited conformance gaps as one mini-RFC; multicast/SHA-256/reorder-buffer/RecvContext/vendor-options are non-goals.
- 3 pre-existing latent bugs folded in: negotiated `timeout` never applied (#16 class); dup re-ACK echoes wrong block; `effectiveTimeout` discards negotiated value after first RTT.
- **Round 2 correctness fixes:** D6 single-fire mid-window / double-fire at boundary (naive double-fire contaminated sender dupAcks); D1 netasciiReader = 2-arg `(blockNum,blocksize)` matching readData (no sendBlocks change), read-ahead until ≥blocksize OR 0-byte EOF (else false-EOF truncation); D2 dally needs `recvOnce` extraction (recvPacket polarity incompatible); D4 = one `validateAndParseOack` → flat `OackOutcome` (not case-object), applyOack consumes filtered result (enforcement); D5 effectiveTimeout floor + seed default from limits.timeout; D7 bounds live in `protocol.nim` (not transfer.nim) w/ re-export.
- Slice 0 tests → `t_transfer`+`t_session` (no `t_server_config`); ServerConfig validated at `startServer` + `handleRrq/Wrq`.
- Land straight on `main` per [[commit-to-main-always]]; verify each slice in Docker per [[no-local-builds-ci-verifies]]; watch [[never-throw-defect-hazard]] on the uint16 underflow + the flat OackOutcome.

## Review ledger (stage 4)
- **Round 1 complete.** 5 reviewers (Correctness, Security, Design, Quality, Nim/FFI/Defect) + 4 adversarial verifiers. FFI-free constraint: CLEAN (0 matches). Every behavioral finding sent to verification HELD — 0 refuted. Awaiting Corey's go on the fix mandate.

| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| R1-1 | High | `recvOnce` while-loop absorbs decode-fail / wrong-TID packets on a fresh full `timeoutMs` each iteration, never rechecking wall-clock; `dallyAfterFinalAck` deadline checked only between calls → remote flood keeps dally (and server slot) open indefinitely, defeats R5 | **fixed** | `recvOnce` now bounds its loop by one absolute wall-clock deadline (shrinking remaining budget → `transport.recv`; budget exhausted → `RecvOutcome(ok:false)`). Regression suite in t_transfer "recvOnce R5 deadline" (RED→GREEN): decode-fail + TID-mismatch floods, strictly-decreasing timeouts, bounded termination. Green: t_transfer, t_props_transfer, t_session |
| R1-2 | High | Client `applyOack` does `xferConfig.timeout = neg.timeout` unconditionally; `validateAndParseOack` seeds `defaultNegotiated().timeout = DefaultTimeout(5)`, only overwritten if OACK carries `timeout`. Server omitting timeout (RFC-legal) → client silently drops configured 30s→5s | **fixed** | `validateAndParseOack` gained `configuredTimeout` param, seeds negotiated timeout from it (mirrors server's `limits.timeout` seed); both engine call sites pass `config.timeout`. t_client "OACK omitting timeout must not clobber…" RED→GREEN. Green: t_options,t_client,t_props,t_server,t_session |
| R1-3 | High | `finishNetasciiDecode` does `discard file.writeBytes(tail,...)` — terminal netascii tail write result dropped; callers gate on `writeError` going in but nothing catches a failed final write → silent truncated file reported `success:true` | **fixed** | New `writeNetasciiTail` injectable-sink seam; `finishNetasciiDecode` returns bool; both `handleWrq` + `tdGet` set `writeError` on false → failure path. t_netascii seam+return-value suites RED→GREEN; happy-path lone-CR guard green |
| R1-4 | Med | `negotiateServerOptions` blksize clamp `max(minBlocksize, min(max, bs))` has no floor at requested → with `ServerConfig.minBlocksize>8`, client requesting smaller blksize gets OACK LARGER than requested (RFC 2348 violation); new client `validateAndParseOack` rejects `bs>reqBs` → own client fails the transfer | **fixed** | blksize now only clamps DOWN to maxBlocksize; requested < minBlocksize → option OMITTED (fall back to 512, mirrors R6 drop), never raised. t_options "requested blksize below server min is omitted…" RED→GREEN; buggy pre-existing clamp-up test rewritten |
| R1-5 | Med | `TransferInfo.errorCode` (Q1) only populated on the 2 new option-neg paths; 4 pre-existing failure sites (`checkWriteAccess`, `validatePath`, `fileExists`, `sendOsErrorAndFail`) send a real `TftpErrorCode` on the wire but drop it from `failResult` → `errorCode==0`, contract unreliable | **fixed** | `ord(code)` threaded into `failResult` at all 4 sites; t_server tests now assert exact wire code (errFileNotFound/errAccessViolation/errFileAlreadyExists/errDiskFull) RED→GREEN. Green |
| R1-6 | Med | Netascii seam is shallow vs the rest of the RFC: `NetasciiPolicy` relabels one boolean (`mode==tmNetascii`) 5×; compound AND at `server.nim:354`; send-reader branch+octet-closure duplicated `server.nim` vs `api.nim`; no decode-side counterpart to `netasciiReader` (feed+finalize footgun hand-rolled at 2 sites); `options.nim` imports `netascii` for one bool (layering leak) | **fixed** | `makeSendReader`/`makeRecvSink` deep constructors own the mode choice; both send sites → 1-2 lines, both recv `onData` → `if not recvSink(data, isFinal): writeError=`. `negotiateServerOptions` takes `suppressTsize: bool` (netascii import removed). `NetasciiPolicy` 5→3 fields (kept skipSidecar/suppressTsize/reportTotalUnknown). Named `shouldDigestForSidecar` predicate. Full suite green (t_security cleanup flake only) |

**Round-1 batch verified:** full `dev-test.ps1` green across all 15 relevant suites; `t_security` only fails its Cleanup junction-removal step (pre-existing flake, 23/23 assertions pass).

### Round 2 (re-review of fix deltas)
Design+Nim: refactor clean, no regressions (Lows only: api.tdGet lazy `recvSink` nil-invariant not type-enforced; R1-9 doAssert still open). Security: found R2-1 High. Correctness: found R2-3 High (fresh regression from R1-2's fix). Everything else in the R1 fix set verified correct.

### Round 4 (re-review of R3 fix delta: `awaitHandshakeReply` helper) — FLOOR REACHED
Security: DoS stays closed, no new surface, no findings. Correctness: faithful extraction, 0 findings — noted the extraction *incidentally fixed* two latent bugs (old loop reseeded from raw unclamped timeout; old code could permanently downgrade blocksize on a spurious off-target packet). Design/Nim: extraction is deep & clean, FFI/Defect-safe; **one Low** (below):
- **R4-1 (Low):** `hoCancelled`/`hoTimedOut → TransferResult` translation (~10 lines) duplicated verbatim in getFile & putFile; helper could return the ready failure `TransferResult` itself (info in `xferConfig`). Fold to finish the dedup.
**Round 4 = 0 Critical/High/Medium → fix loop terminates at the floor.**

### Remaining Lows — ALL FIXED (Corey opted to clean them up before commit)
- **R1-9 fixed:** `netasciiReader` `doAssert` → new `NetasciiReaderError (CatchableError)` + `raise`; verified it fails `sendBlocks`' Future cleanly (both `tdPut` and server `run()` already treat a failed Future as an ordinary outcome). New t_transfer test + updated t_netascii invariant test. No Defect left on the path.
- **R2-2 fixed:** removed the redundant `gfileOpened` flag; `recvSink != nil` is now the single source of truth for "file open + sink ready" — open and sink-construction can't desync. NOT via `doAssert` (that would add a Defect).
- **R4-1 fixed:** `HandshakeOutcome` gained a `failure: TransferResult` field built once in `awaitHandshakeReply`; both callers collapsed to `of hoCancelled, hoTimedOut: return handshake.failure` / `of hoGot: discard`. Error strings byte-identical.
- **Misc fixed:** `sendAck` blocksInWindow-reset comment; `recvPacket` RTT-sampling comment; `netasciiReader` `var`→`sink NetasciiEncoder`; `encode(rrq/wrq)` computed once; `fillWindow` wrap `# NOTE` (pre-empted by existing high(uint16) ceiling); `awaitHandshakeReply` `wanted` non-nil doc note.
- **Verified:** full `dev-test.ps1` — 15/16 suites green, `t_security` only its Cleanup junction-removal flake. FFI-clean; no new Defect construct on any network/public path.

**All findings (12 above-floor + all Lows) resolved. Ready to commit the full RFC to `main` as one batch.**

### Round 3 (re-review of R2 fix deltas: engine handshake + timeout clamp)
All 3 reviewers: R2-1/R2-3 fixes themselves verified correct, DoS closed, FFI-clean, no Defect. Two new Mediums (corroborated across reviewers), both in engine.nim:

| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| R3-1 | Med | Handshake `deadline` math at 6 sites (engine.nim getFile 101/116/129, putFile 232/248/261) seeds from RAW `config.timeout`, not clamped `xferConfig.timeout` — R2-3 fixed the seed line but not the deadline lines above it. Direct engine caller with timeout=0/300 → unclamped deadline (0 → instant-fail busy-loop; 300 → 300000ms to socket). Masked today (api.startTransfer clamps; sole prod caller) but breaks single-clamped-source invariant; RFC #17 embedding API is a potential direct constructor | **fixed** | All 6 sites → `xferConfig.timeout.float`; grep confirms no `config.timeout` feeds deadline math. t_client: assert `recvTimeoutsMs[0]==255000` + new timeout=0 handshake-sanity test RED→GREEN. (Now lives inside the R3-2 helper.) |
| R3-2 | Med | Absolute-deadline retry loop duplicated near-verbatim getFile↔putFile (deadline init/remainingMs/timeout-catch/exhaustion/reseed); author noted parity in a comment but didn't extract → will drift | **fixed** | Extracted `awaitHandshakeReply(transport,xferConfig,host,port,requestPkt,cancelCheck,wanted): Future[HandshakeOutcome]`; flat outcome (hoGot/hoTimedOut/hoCancelled); `wanted` classifier preserves off-target-within-budget; deadline seeded from clamped timeout. ~55 dup lines removed from both procs; callers keep only opcode dispatch. 5 suites green unchanged. |

| id | sev | finding | status | proof / reason |
|----|-----|---------|--------|----------------|
| R2-1 | High | R1-1 fix scoped to `recvOnce`/dally only; `engine.nim` `getFile`/`putFile` pre-handshake loops poll `transport.recv` directly with the SAME unbounded pattern — decode-fail (engine.nim:110-111/212-213) & off-target block (150/234-235) `continue` without budget/deadline; fresh full timeout each recv; no outer `withTimeout` in `api.startTransfer`; no TID needed (floods ephemeral port post-RRQ/WRQ). `putFile` worse — no cancel check. | **fixed** | Both handshake loops now use one absolute wall-clock deadline per retry attempt (shrinking remaining → `transport.recv`; garbage consumes budget, not resets); exhaustion/timeout advances `retryCount`+resend+reseed. `putFile` gained cancelCheck parity. t_client flood tests (get/put decode-fail + off-target) RED→GREEN. Green: t_client,t_api,t_session,t_server,t_transfer |
| R2-2 | Low | `api.tdGet` lazy `recvSink` is `nil` until first DATA; correct today but invariant not type-enforced (future reorder → NilAccessDefect) | open | Design reviewer; suggest `doAssert recvSink != nil` or open+construct in one helper |
| R2-3 | High | **Fresh regression from R1-2 fix:** `engine.nim:115/217` pass raw unclamped `config.timeout` as `configuredTimeout` seed (not the clamped `xferConfig.timeout` one line above); `api.startTransfer` doesn't clamp timeout (blocksize/windowsize are) → out-of-range/0 timeout leaks via OACK-omit path → `recvOnce` deadline `epochTime()+0` → instant fail | **fixed** | engine now seeds from `xferConfig.timeout` (clamped) at both sites; `api.startTransfer` clamps timeout inline (matches blocksize/windowsize). t_client: engine-level (300) + public-path (0 and 300) tests RED→GREEN. Green (same 5 suites) |
| R1-7 | Med | `parseOackOptions*` has zero production callers (D4 replaced it with `validateAndParseOack`); only tests call it | **fixed** | Removed proc + 12 orphaned t_options tests + t_props property (+ unused helpers) + stale doc-comment; coverage already in `validateAndParseOack` suite. Green |
| R1-8 | Med | No wire/session-level test for R6 server timeout-drop (RRQ/WRQ with `timeout=0`/out-of-range → OACK omits it); only the pure-fn `negotiateServerOptions` case is tested | **fixed** | Added t_server suite "handleRrq/handleWrq — R6: out-of-range timeout dropped from OACK" (RRQ timeout=0/300, WRQ timeout=0; OACK negotiates blksize, omits timeout). Pure test add, green. |
| R1-9 | Low | `netasciiReader` `doAssert` (never stripped in release) sits in a network-facing path with no `except CatchableError` net; invariant holds today (windowCache replays, never re-reads) but a future regression → uncatchable AssertionDefect | open | Nim reviewer; not currently wire-triggerable |
| R1-10 | Low | Misc: api.nim tdGet octet-path final-flush asymmetry vs server; `sendAck` now resets `blocksInWindow` for dup/gap ACKs (undocumented cadence change, windowsize>1); `recvOnce` shifts RTT-sample timing (not quite "behavior-preserving"); `netasciiReader` by-value encoder capture → could be `sink`; `fillWindow` uint16 wrap at >65535 blocks (pre-existing) | open | Quality+Nim Lows; comments/notes mostly |
