# Auto-loaded by nim when compiling tests/t_symex_security.nim. Adds Z3's
# headers to the C compiler's include path for softlink's compile-time
# _Static_assert. The chapulin-symex image (Dockerfile.symex) installs them
# at C:\z3\include. Mirrors t_symex_decode.nim.cfg (forward slashes: nim's
# cfg parser treats backslashes as escape sequences).
--cincludes:"C:/z3/include"
